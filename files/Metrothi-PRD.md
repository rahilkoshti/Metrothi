# Metrothi — Product Requirements Document & Technical Blueprint

**Version:** 2.0 · **Date:** 2026-07-15 · **Owner:** Rahil
**Status:** Master reference. All future build work should trace back to this document. Changes to scope/architecture get made *here first*, then implemented.
**Last updated:** 2026-07-28 — station & passenger information scraped from GMRC (gates, lifts, multi-modal interchange, rules, facilities, contacts) and given a delivery plan: Station Info tab blocks (§4.4.1), reference pages under YOU (§4.5.1), last-mile arrival guidance (§4.2), the data layer and its sourcing rule (§5.6), and a phased rollout (§8). See Change Log at the bottom.

---

## 1. Product vision

Metrothi is the ultimate, fully-functional companion application for the Ahmedabad Metro system. Built for speed and utility: open it, and within two seconds you know **which station to walk to, when the next train actually leaves, and exactly how to get where you're going** — including what ticket works, where to change lines, and whether the trip is even possible right now.

**One-line pitch:** A production-ready, lightning-fast, offline-capable journey planning platform for the Ahmedabad–Gandhinagar metro.

### Guiding principles
1. **Honesty over illusion.** GMRC publishes no live vehicle feed. We simulate from the real timetable and explicitly say so. If a trip can't be completed today, we say "not possible," never a fabricated ETA.
2. **Frictionless Onboarding.** The app works immediately without forcing a login. GPS improves the experience but is never required. 
3. **Local-first & Offline-ready.** Core features (nearest station, journey planning, schedule estimates) must work seamlessly offline (Progressive Web App architecture).
4. **Cloud-Synced for Power Users.** Once logged in, users can sync saved journeys, frequent places, and preferences securely across devices.

---

## 2. Users & core jobs-to-be-done

| User | Job |
|---|---|
| Daily commuter | "Get me to work; tell me when to leave, track my exact current location in transit, and sync my saved routes." |
| Occasional rider | "I don't know the network. Which station, which line, which ticket, how much?" |
| Cross-city traveler (Ahmedabad ↔ Gandhinagar/GIFT City) | "Is this trip possible right now, what does it cost, and why doesn't my token work?" |
| Visitor/tourist | "I know my destination *place* (e.g. Narendra Modi Stadium), not a station name. Route me anyway using multimodal suggestions." |

---

## 3. Scope

### In scope (Full Production Features)
- **Core Routing:** Nearest-station detection (GPS), schedule-based live estimates, multi-transfer routing, feasibility checking, fare calculation.
- **Progressive Web App (PWA):** Installable on iOS/Android, fully capable of offline routing and schedule lookups.
- **Backend & Auth (Supabase):** Secure user authentication (Email/Google/Apple) with Postgres RLS securing user data.
- **User Profiles:** Cloud synchronization of saved places, saved daily commutes, and user preferences.
- **Destination Resolving:** Integration with MapLibre/Google Places to resolve point-of-interest names to the nearest metro station.
- **Journey State Machine:** Live turn-by-turn tracking when a user starts a journey (Walking → Waiting → On Train → Transferring → Arrived).
- **Push Notifications:** Service-worker-based notifications (e.g. "Leave in 10 minutes to catch the 9:45 AM Blue Line train").
- **Multilingual UI (English / Hindi / Gujarati):** Full interface localization with a language switcher in settings, plus station, line, and place names localized from official GMRC naming. Planned — see §6.

### Explicitly out of scope
- Real-time vehicle positions (no data source exists yet; architecture must support it *when* it drops).
- Ticket purchase / NCMC recharge (No GMRC API available; do not build financial transacting logic).

---

## 4. Information architecture — map-first, overlay-driven

The app is a single map-first surface rather than a tab-bar shell (the nav bar
is currently suppressed — `--nav-h: 0`). HOME *is* the live map; everything else
— search, the GO planner, station detail — floats over it as an overlay so the
map never unmounts and no page transition is needed. YOU (settings) is the one
genuine route. The GO / MAP / STATIONS / YOU descriptions below define those
surfaces regardless of how they're currently mounted.

```text
HOME (the map)  ── overlays ──▶  SEARCH · GO planner · station sheet
                └─ route ──────▶  YOU (settings)
```

### 4.1 HOME — live map dashboard
- **Map as the surface:** The home screen is the full-bleed live map (§4.3), not a card list. Floating over it: a search pill ("Search stations and landmarks"), a settings button, and the line-status strip.
- **Nearest-station sheet:** A draggable bottom sheet anchored to the user's nearest station (or a default when location is denied). Three snaps — collapsed peek → mid → full — but the sheet **opens at mid**, not at the peek: the station's actions and next departures are the screen's primary content and shouldn't cost a drag.
- **Sheet header (sticky):** A **station photo** (see §4.6), an accent eyebrow ("Nearest station" / "Default station" / "Station"), the station name, and — right-aligned — a **walking-directions** icon button and a **save** toggle that persists to `localStorage['metrothi-saved-stations']`. There is deliberately **no line badge**: the first chip below already names the line, and the badge only repeated it.
  - **Walking directions** (shown only at the user's *nearest* station, where the walk is the next thing you actually do) hands off to **Google Maps walking directions** — street-level navigation is not something this app models. Origin is the live fix where available; Google Maps falls back to the device's own location otherwise. It stays a quiet icon rather than a button so it doesn't compete with Start Journey below.
  - **Save** is a bare bookmark icon when unsaved — the icon alone reads as "save" — and widens into a labelled "Saved" pill once set, so the state is confirmed in words.
- **Chip row:** **line · walk distance · walk time · Interchange**, all visible at **every** snap including the collapsed peek. Service status is folded into the line chip itself rather than carried as a separate pill: a running line shows its name alone, a line that isn't appends the reason ("Starts in 12m", "Service ended", "Bus only") in the same alert-toned pill. The structural and status chips used to be gated behind an expanded sheet to hold the row to one line; the peek now **grows with its header** (`max(collapsedHeight, headerH)`, base 118px), so a wrap costs nothing and nothing is cut off at the fold.
- **Upcoming Trains preview:** The first thing in the sheet body — above the action, since the departures are what you came for. The next departure in each direction, for every line the station serves (2 cards at a through-station, 4 at an interchange, 1 per line at a terminal), rendered with the shared `DepartureRow` (§4.6). A green "Live" marker appears while any of the station's lines is running. **View all →** opens the full station page.
- **Sheet body — action:** A single primary **Start Journey** button below the departures. Which end of the trip the station fills depends on proximity: at the **nearest** station you're standing at the origin, so it seeds the *source*; anywhere else you're looking at somewhere you want to get to, so it seeds the *destination*. Either way it fires the in-app `home-plan-trip` event, so the planner opens as an overlay with the map still mounted beneath.
- **Merged schedule:** Below the preview, each line's full-day schedule lists **both directions in one time-sorted list** (no direction tabs), greying past departures and highlighting the next train each way. Tapping any train opens its full stop-by-stop route.
- **Sheet sizing & gestures:** The mid snap is **fitted to its own content**, not a fixed fraction — it measures the departures + action block and rests exactly at that block's bottom edge, so the schedule below never peeks through the fold on a normal station. It's floored so the sheet can never take more than **~60% of the screen** — raised from an earlier ~88% cap, which on a 4-card interchange left too thin a map strip for the floating controls (see DISCREPANCIES.md, resolved 2026-07-28); a big interchange's departures now scroll within the mid snap rather than all fitting above the fold. A tap on the header **climbs** the ladder (collapsed → mid → full, full → mid); a tap on the **map surface collapses** the sheet to its peek. Leaflet suppresses the map tap after a pan, and station markers don't trigger it — tapping a marker selects that station and raises the sheet to mid.
- **Map framing:** The map's centre nudge tracks the sheet's *resting* height rather than the collapsed peek, so the selected station's marker lands in the middle of the exposed map band instead of hugging the sheet's edge.
- **Map hidden while covered:** When nothing of the map is on screen — the sheet fully raised, or the search / planner overlay open, all three being opaque and full-bleed — the map is hidden outright (`visibility: hidden` on a wrapper, so no paint and no compositing) and its live-train ticker is stopped (§4.3). Two rules make this safe:
  - The map **never unmounts**. Remounting Leaflet would drop the user's pan and zoom, and `visibility` preserves its size, so there is nothing to restore on the way back. The wrapper's background matches the sheet's, so the strip under the sheet's rounded top corners stays seamless.
  - "Covered" is read from the sheet's **live position**, not its `snap` prop (`DraggableSheet`'s `onCoverageChange`). The map therefore stays painted for the whole drag or spring and only goes dark once the sheet has actually arrived — gating on `snap` would blank it the instant the animation *started*.
- **Service status strip:** Degraded-state pills for any line not currently running; tapping one opens search focused on that line.

### 4.2 GO — the heart of the product
- **From/To inputs:** Intelligent search resolving both station names and generic places (via Geocoding).
- **Results screen:** Stop-by-stop list, total transfers, fare, total time. Fare is computed from real GMRC distance data (§5.4), not an approximation, and is shown as `—` rather than a guess if a route's distance can't be resolved.
- **Summary sheet (mid snap):** The planned trip's summary lives in the home sheet, built around **one hero figure and one supporting column** rather than a row of equal stats.
  - **Hero — "Leave in N"** (or "Leave · Now", or "Leave now — tight" in amber). The rider has exactly one thing to do next, so the countdown to it gets the only large number. Urgency reads off the countdown itself; colour only intervenes for a tight connection, where "now" isn't quite enough.
  - **Beside it — the trip as a span:** `depart → arrive`, with **time actually spent on a train** underneath and the fare appended. Only the arrival carries a meridiem — the pair almost always shares one. Laid out as two columns on one row rather than stacked, because stacking cost ~70px and pushed Start Journey below the sheet's mid snap.
  - **Departure picker:** a **fixed four-cell segmented row**, not a scroller — four cells always fit, so nothing is clipped by the fold and it doesn't compete with the sheet's own vertical drag. Each cell is only the clock time; the countdown lives once, in the hero this control drives. The meridiem is printed timetable-style, only when it changes. Difference is carried by **colour, not opacity** (a dimmed cell reads as disabled, and infeasible and tight options are both still selectable). It renders even when the current selection is infeasible — that is precisely when the rider needs to switch.
  - **Walk disclosure:** door-to-door plans append "Includes N walking", summed rather than split by end, since the route timeline below names each walk in full.
  - Arrival is **not** repeated in the sheet header above: the header owns the route's *static* facts (stops, transfers), the summary owns the chosen departure's, which change under it every time the picker moves. Carrying arrival in both printed the same clock time twice, one above the other.
- **Ticket guidance card:** Advises on Token vs CSC vs NCMC requirements based on crossed phases, and notes the CSC/NCMC 10% discount (confirmed, §5.4) — display only, never folded into the fare shown. Rendered as a quiet aside rather than a third coloured block competing with the picker and the CTA. Where it tells a rider they need an NCMC, it now says **where to get one** on a second line: *"GMRC lists NCMC cards as available at every station; banks issue them too."* Without it the card is a dead end — our own data describes NCMC as a bank-issued product, so "only an NCMC works" reads as *nothing you can do here*, when GMRC's MMI page says the station sells them. The second line renders **only on that branch**: under "Token, Smart Card, or NCMC all work", purchase advice answers a question nobody asked. It is a separate line rather than a fourth sentence because the note is the constraint and this is the remedy, and at 11px they stop being separable once run together.
- **Arrival & last-mile guidance:** The destination station's exit information is the natural close of a journey, and it is the one place the physical-station data (§5.6) is *decision-making-in-the-moment* rather than reference. The results screen's destination row and the live journey's final stop both carry the destination's step-free exit and — on the stations that have one — its onward connection: *"Step-free exit at Gates 2, 3 & 5"* over *"Gate 3 · Lift and Skywalk connecting BRTS"* at Sabarmati. This is precisely the "first- and last-mile connectivity" GMRC's MMI page exists to describe, and it lands at the moment the rider needs it rather than in a settings page they'd have read yesterday. GMRC's own wording is kept, minus the Entry-Exit prefix the gate beside it already states; rewriting it into a phrase of our own would be invention (§7.6). Suppressed silently where the station has no published connection — same no-false-negative rule as §4.4.1.
- **Warning cards:** Feasibility warnings for last-train cutoffs or bus-only windows. An infeasible selection replaces the hero with a "Route Not Possible" card naming the line you'd be stranded at, and the Start Journey CTA is withheld.
- **All trains list:** Every departure for the day below the summary. Rows show the **train's own departure time** (matching the picker above, not the door-to-door leave time, which is a minute or two earlier and made one train read as two) and the **ride time** (§5.5, not `totalMins`).
- **Journey Mode:** Actively transitions UI state as the user progresses geographically through their trip.

### 4.3 MAP
- **Interactive geographic map:** High-performance vector map (MapLibre/Leaflet).
- **Real track geometry:** Line polylines are drawn from actual OpenStreetMap rail geometry (`app/src/data/tracks.json`, `map/geometry/trackGeometry.ts`), not straight station-to-station chords. Each line degrades gracefully to the chord if track data is missing.
- **Snapped station markers:** Station markers are projected onto the drawn track (via each station's `stationKm`) so they sit *on* the line rather than beside it. Terminal (end-of-line) markers are rectangular "buffer caps" rotated to sit **perpendicular to the track direction** at that point, computed in Web Mercator so the angle matches the rendered polyline.
- **Live simulated trains:** Schedule-derived trains glide along the real track (`LiveTrainsLayer`), interpolated by segment progress, updating every second. Each train marker carries a **direction-of-travel arrowhead** pointing the way the train is heading, so the map reads as live movement. (Positions remain simulation from the timetable, per §1 principle 1 — no live feed is implied.)
  - **Paused while covered:** rebuilding every train's SVG once a second is the map's only continuous cost, so the ticker stops whenever the map can't be seen (§4.1), and a pan or zoom while paused doesn't redraw either. Resuming **ticks immediately** rather than waiting out the interval — positions held over from before the pause are stale by however long it lasted. The pause is driven by the owner, deliberately **not** by `document.visibilityState`: browsers already throttle background timers hard, and the dev preview reports itself hidden while plainly on screen, which would leave the map trainless throughout.
- **Constrained viewport:** The map is fenced to the metro's footprint so users never wander off into empty map. Implemented with Leaflet `maxBounds` (the network bounding box padded ~25%, i.e. Ahmedabad through Gandhinagar/GIFT City) + `maxBoundsViscosity: 1.0`, so the drag stops solid at the edge (a hard fence, no bounce-back). Zoom is floored at `minZoom` (can't zoom out past seeing the whole network) and capped at `maxZoom: 18` (free to inspect any section up close). Panning is unrestricted *within* the box at any zoom.
- **Dynamic highlighting:** Dimming non-relevant lines and highlighting the active path during route planning.
- **Station Bottom Sheet:** Quick actions directly from the map viewport.

### 4.4 STATIONS
- **Directory & Live Details:** Grouped by line with interchange and phase badges, and a **connection facet** over `stationsWithMode()` (§5.6) — one chip per mode, carrying its own count, filtering the directory to the stations GMRC names as an interchange with it. Nine stations, four modes (PDEU's `multiModal` is a parking amenity with no mode, so it is in the Connections block but not in a facet). Single-select: "BRTS *and* railways" has no useful reading, and no two modes here overlap on more than one station. Every station's modes also ride in its row's meta line, filtered or not — that is what makes a keyword result legible (below).
- **Search keywords:** `fuzzySearch.ts` matched station names only, so a visitor searching *"bus"* found nothing, though six stations physically connect to one. Each station's `multiModal.modes` now feed in as **non-displayed keyword aliases** — every alias tier scores below the weakest name tier, so an alias can add a result but never reorder one — and "BRTS", "bus", "railway" resolve to real stations on both search surfaces (the home overlay and the journey planner). This directly serves the visitor persona (§2), who knows the mode, not the station name. Aliases deliberately do **not** match by scattered subsequence the way names do: a keyword is a word you either typed or didn't, and "brs" matching BRTS would drag the interchange stations into every three-letter query.
- **Station Detail Pages:** `/stations/:id` shows all live simulated departures per line. It is a real route (the SEO/deep-link surface) and is reached from the sheet's **View all →**. The same full-day schedule also lives further down the home sheet at its full snap, so the two are alternative ways to the same content — see the open item in `DISCREPANCIES.md`.
- **Shared body:** `StationDetailBody` renders the schedule for both the standalone page and the home sheet, so the two can't drift. Two flags adapt it: `showHero` (the sheet already names the station in its header) and `showActions` (the sheet supplies its own Start Journey action, so the page's **From here / To here** buttons are suppressed there). Phase and stop-of-N remain on the standalone page's hero; they were dropped from the sheet's chip row.
- **Merged schedule anchoring:** the full-day list parks the first still-upcoming train at the **top** of the scroller and keeps it there as departures roll past. The offset is derived from bounding rects rather than `offsetTop`, which is measured from the nearest positioned ancestor and not the scroller, so it stays correct however the sheet above is laid out. The first anchor is a **jump**, not a smooth scroll — animating from an arbitrary mount position is what reads as "the list loaded mid-scroll".

#### 4.4.1 Station Info tab — physical station data (planned)

The **Station Info** tab (the second of `StationDetailBody`'s two tabs, beside **Schedule**) currently shows only what the topology and the timetable already knew: line count, phase, stop-of-N, operational status, interchange/terminus, and today's first/last train. `stationFacilities.json` (§5.6) adds the station's *physical* facts, which is what someone standing outside it actually needs. Three new blocks, in this order — nearest-term need first:

1. **Entrances.** Which numbered gates are open (`gates`), rendered from `formatGateList()`. This is the highest-value fact in the whole dataset: at Old High Court six of eight gates are open and at Ranip only three of four, and picking the wrong one costs a road crossing. Gate **numbers match the signage**, which is why GMRC's numbering is reproduced verbatim rather than re-indexed to 1..n.
2. **Step-free access.** The gates with a lift (`accessibleGates()`), named with GMRC's own lift numbers so they match the signage too. Stated **positively** — "Step-free entry at Gates 1 & 3" — never as "Gates 2 and 4 have no lift": GMRC publishes where lifts *are*, and inferring their absence elsewhere from that is a claim the source does not make. Every one of the 53 listed stations has at least one lift, so this block always renders when the station is listed.
3. **Connections.** Only on the **10 stations** that have one (`multiModal`): a mode chip per `modes` entry (BRTS / GSRTC buses / Indian Railways / High-speed rail) over GMRC's verbatim `connections[].text`, each tied to the gate it uses — "BRTS · Lift and Skywalk at Gate 5" at AEC. Because `connections[].gate` is validated at scrape time against the same station's operational gates, this cross-references block 1 rather than contradicting it. The block is keyed off having *something* to say, not off `modes`: PDEU carries an `amenities` entry (parking, from a rendering's caption — so it shows GMRC's `sourceNote` in fine print) with no interchange at all, and keying off modes would drop its one published fact. `plannedAmenities` is not rendered, since planned is not built.

**Structure** (`elevated` / `underground`) joins the existing Station Overview tiles — replacing nothing, since it answers a different question from the four already there, and it matters: four stations are underground, where the street-to-platform walk is longer and GPS dies at the concourse.

**Absent data is absent.** 43 stations have no `multiModal` and the Connections block simply does not render for them — no "No connections nearby" line, which would be a false negative (GMRC publishes *built interchanges*, not the presence or absence of bus stops). `sabarmati-railway-station` returns `null` from `stationFacilities()` entirely; it is not yet operational, the Status tile already says so, and all three blocks close up. This is the `stationImages.ts` rule (§4.6) applied to facts instead of photos.

**Chip row.** `multiModal.modes` also renders as **one chip per mode** in the sheet's chip row (§4.1) — "BRTS" at Vadaj tells a rider something that changes their plan, and it appears on only 9 stations so it never crowds the other 45. PDEU shows none: its `multiModal` is parking, which is an amenity, not a way onward (the same call phase 3 made for its exit guidance).

The 375px gate this was held behind is **passed, and its premise has since changed**: the peek can no longer push a wrapped row under the fold, because `DraggableSheet.pointsFor` takes `max(collapsedHeight, headerHeight)` — a header that outgrows the caller's constant grows the peek instead of being cut off. So the real cost of a wrap is map area, not hidden content. Measured at 375px, per station, in each of the four states the row has (line running or closed × nearest or not), the chips add a line only here:

| | line running, not nearest | line closed, or nearest |
|---|---|---|
| **6 one-mode stations** | no change (1 line) | Gandhigram, APMC and Mahatma Mandir gain a line while nearest |
| **Kalupur, Ranip** (2) | no change (1 line) | gain a line unless the row already wrapped |
| **AEC** (3) | **1 → 2 lines** | 2 → 3 in the worst cell (nearest, line closed) |

A wrapped line costs 36px of header, which the peek absorbs as 30px (the 118px constant carries 6px of slack): the collapsed sheet stands 148px instead of 118px and leaves 642px of map instead of 672px. AEC is the only station that pays it in the state you most often browse in. At 320px — below the gate, and not a size the rest of the app is tuned for either — AEC reaches 3 lines and Kalupur 2; nothing clips, the peek simply grows.

A single rolled-up chip ("BRTS +2") would fit everywhere and was rejected: naming the mode is the entire value of the chip, and a roll-up that hides two of three names at the one station with three is exactly backwards.

### 4.5 YOU
- **Authentication:** Login/Signup flows powered by Supabase Auth.
- **Data Management:** Sync settings, clear local data, manage saved commutes and places.
- **Language:** A three-way selector (English / हिंदी / ગુજરાતી) sets the app-wide interface language, styled like the existing theme toggle. Planned — see §6.6.

#### 4.5.1 Reference content — "Riding the metro" & "Help" (planned)

Everything in `passengerInfo.json` and the non-fare half of `metroInfo.json` (§5.6) is **reference content**: read rarely, but the app is worthless to an occasional rider or a visitor (§2) without it. It belongs in YOU rather than on the map, because none of it is decision-making-in-the-moment data — with one exception noted below. Two new sections, using the existing `SectionHeader` + `Row` primitives:

**"Riding the metro"** — five rows, each opening one reference page:

| Row | Source | Why a rider opens it |
|---|---|---|
| **Fares & ticket rules** | `metroInfo.json` — `fareMedia`, `fareProducts`, `purchase`, `ticketValidity`, `timeInPaidArea`, `concessions`, `discounts`, `refunds`, `phaseRestriction`, `ticketConditions`, `penalties` | "Which ticket, bought where, valid for how long, and what happens if I get it wrong." Answers the cross-city traveler's *"why doesn't my token work?"* (§2) at leisure, where the GO ticket card (§4.2) only warns in passing. |
| **Do's & don'ts** | `passengerInfo.dosAndDonts` — 9 dos, 14 don'ts | First-time riders. Several are genuinely non-obvious (no eating in the paid area, no sharing one ticket across a group). |
| **Prohibited items** | `passengerInfo.prohibitedItems` — dangerous, offensive, live animals, each with its exception, under the Metro Rail (O&M) Act 2002 | "Can I bring this." The pets answer alone justifies the row. |
| **Facilities & accessibility** | `passengerInfo.facilities` — 10 general, 8 accessibility | Network-wide, deliberately split from the per-station data. Ends with a pointer to the Station Info tab (§4.4.1), which is where lift-by-gate lives. |
| **Safety & emergency** | `passengerInfo.emergencyFacilities` — 11 items grouped by `location` (platform / station / train / both) | Grouped by *where the thing is*, not listed flat: "on the platform" and "in the train" are how you'd look for it. |

**"Help & contact"** — four rows:

- **Customer care** — `contact.customerCare`, with the phone and email as **`tel:` and `mailto:` actions**, not copyable text. Carries GMRC's own scope note (operational matters only) so nobody emails passenger care about a tender.
- **Lost & found** — `lostAndFound`. Office, hours, `tel:`/`mailto:`, the four claim rules, and the six-month disposal deadline. The live found-items list is **not** bundled (it changes weekly); the row links out to `listingUrl` instead.
- **Official GMRC app** — `officialApp`, platform-aware. Metrothi does not and will not sell tickets (§3, out of scope), so the honest move is to name the app that does rather than dead-end the user.
- **GMRC on the web** — `officialLinks` (12) and `social` (5). Every reference page also carries a "Read the original on gujaratmetrorail.com" footer link to its own source, so a rider can always check us against GMRC.

**About & Data** gains a provenance row for this data — source and `_meta.scrapedOn` — matching the existing timetable/fares/live-estimates rows. This is principle 1 (§1) applied to reference content: the rider can see how old it is.

**Feedback** keeps its two existing rows and gains GMRC's `feedbackUrl`, clearly separated: reporting a *timetable error in Metrothi* goes to us, a complaint about *the metro* goes to GMRC.

**Considered and rejected — emergency info outside settings.** A quiet "Emergency" affordance on the Live Journey screen (§4.2) was considered and dropped. Reading a list of equipment on a phone is not what anyone does in an emergency, and our own source says why: the platform has a stop plunger, the train has a passenger alarm, and the station has a helpline. An app that inserts itself between a rider and those is worse than one that stays out of the way. The content stays informational, in settings, where it's read *before* it's needed.

### 4.6 Shared UI primitives

- **`DepartureRow`** (`features/journey/components/DepartureRow.tsx`) — one departure card: a line-tinted initial badge, "Towards X", a subtitle, and one big figure on the right. Shared by the station sheet's departure board and the station page's full-day schedule so the two can't drift apart. A `primary` prop picks which figure takes the big right-hand slot and pushes the other into the subtitle: the sheet leads with the **countdown** (clock time below), the full-day schedule leads with the **departure time**, since the timetable itself is what you're reading there. Renders as a `button` only when given an `onClick`, a `div` otherwise. Departed rows dim and strike through; the highlighted "next" row is tinted with the line colour.
- **`InfoPage`** (planned, `features/info/InfoPage.tsx`) — one generic reference-content screen, not nine hand-built ones. The pages in §4.5.1 differ only in their content, and that content is already structured in JSON, so they render from a shared block model: `list` (bulleted, e.g. the don'ts), `keyValue` (e.g. luggage limits, penalties), `prose`, `actions` (`tel:` / `mailto:` / external), and `sourceLink`. A registry (`features/info/topics.ts`) maps a topic slug to `{ title, blocks }` built from the JSON, and each topic is a real deep-linkable route `/you/:topic` — reference content is exactly the kind of thing a rider gets sent a link to, the same argument that makes `/stations/:id` a real route (§4.4). Nine screens hand-built against nine JSON shapes would drift; one component against one block model can't. Reference JSON is **dynamically imported** by this route so 12 KB of do's-and-donts prose stays out of the boot path of a map-first app (§5.6).
- **Station photos** (`features/journey/stationImages.ts`) — photos of the stations themselves, keyed by station id. Only stations that have **actually been shot** appear: there is **no placeholder and no per-line stand-in**, because a photo of the wrong station is worse than no photo in a wayfinding app. `stationImage(id)` returns `null` on a miss and callers must handle it — the sheet header simply closes the row up. Assets are square (the slots that use them are), pre-cropped on the station structure, and compressed at build-input time rather than resized in CSS from a multi-megabyte original. The photo is decorative — the station's name sits right beside it — so it carries an empty `alt`. Currently: Doordarshan Kendra.

---

## 5. Journey Engine & Architecture

### 5.1 Network Model & Simulation
- The engine uses a pure TS ordered-array approach for deterministic, zero-dependency routing.
- **Transfer buffer:** Currently a flat 3-minute penalty. Must be upgraded to per-station walking matrixes before v2.0 launch.
- **Simulated Headways:** Engine projects schedule times using frequency bands hand-transcribed from GMRC's official PDF timetables.

### 5.2 Application Architecture
- **Frontend Stack:** React 19, TypeScript, Vite, TailwindCSS (Vanilla CSS for tokens).
- **State Management:** Zustand for local app state, TanStack Query for remote asynchronous caching (Places API, backend sync).
- **Local Persistence:** The transit graph and schedules are **bundled at build time** as imported JSON (`app/src/data/*.json`), so they ship inside the precached JS and need no runtime store — Dexie holds *user* data only, never the graph. User data (saved stations, saved journeys, recent trips, preferences) lives in **Dexie.js/IndexedDB** (`app/src/data/db.ts`), migrated once out of `localStorage`. *This reverses the earlier decision to skip Dexie. That decision was correct on its own terms and its stated condition was "revisit only if user data outgrows localStorage's ~5 MB" — which is **not** what changed. What changed is §5.7: a write that must survive being made offline and be replayed against Supabase later needs a durable queue with indexed reads and atomic transactions, and `localStorage` gives none of those (no indices, no transactions, synchronous main-thread writes, and a read-modify-write of one JSON blob per mutation that loses a concurrent write). The async hydration path the earlier note warned about is real and is the price paid; see §5.7 for how first paint avoids it.*
- **Backend Stack:** Supabase (PostgreSQL, GoTrue for Auth, PostgREST for APIs).
- **Hosting:** Vercel (or Cloudflare Pages) with Edge caching.
- **Telemetry:** Sentry (Error tracking), PostHog (Product analytics with stringent PII stripping).

### 5.3 Local-First Sync Strategy
All transit graph data (stations, lines, schedules) is bundled with the PWA and precached by the service worker, so journey planning and fare calculation run fully offline. User data is written **to Dexie first and to Supabase second** — the local write is what the UI renders, and it never waits on, or fails because of, the network. Sync is described in §5.7.

The load-bearing invariant: **the app is fully usable with no account, no network, and no Supabase project configured at all.** Signing in is an upgrade (your data follows you to another device), never a requirement. If `VITE_SUPABASE_URL` is unset the client is `null`, every sync entry point no-ops, and nothing in the UI degrades except that the sign-in row says so.

### 5.4 Fare Engine
`fareEngine.ts` computes fares from route **distance**, not station count — GMRC's own `get_fare` endpoint (`gujaratmetrorail.com/ahmedabad/wp-admin/admin-ajax.php`, action `get_fare`) confirmed distance is the actual basis: pairs with identical station counts but different distances return different fares.

- **Segment data:** Track distance for all 55 adjacent station segments was read directly from that endpoint and stored in `app/src/data/fares.json`. A cumulative-distance graph built from those segments reproduces GMRC's own station counts on every sampled pair and its published distance to within 2-decimal rounding — including cross-line paths through an interchange (e.g. Gandhigram → Old High Court → Usmanpura).
- **Slabs:** ₹5 minimum, then cuts at 2.5 / 7.5 / 12.5 / 17.5 / 22.5 / 30 / 37.5 km → ₹5 / 10 / 15 / 20 / 25 / 30 / 35 / 40. (The fare previously shipped was station-count-based and capped at ₹25 — it undercharged every trip over ~22.5km by up to ₹15.)
- **Overrides:** GMRC's internal chargeable distance differs from the distance it *displays* by up to ~0.06km, so a handful of pairs sitting almost exactly on a slab cut land on the wrong side of the rule. 6 such pairs were confirmed directly against GMRC and are hard-coded as overrides that win over the slab calculation.
- **Route fare** is computed off the actual ordered station sequence a journey passes through (`journeyEngine.ts`'s `merged` path), not the endpoints alone, so it's correct through interchanges.
- **Verification:** `fareEngine.test.ts` asserts 156 real GMRC-sampled pairs end-to-end through `planJourney` (fixture: `__fixtures__/gmrc-fare-samples.json`). Known gap: only pairs within ~0.12km of a slab cut were checked exhaustively; pairs further from a cut are unverified but low-risk. Full methodology and confidence notes live in `fares.json`'s `_meta` block.
- **NCMC / CSC 10% discount:** Confirmed against GMRC's `fare-rules` page and independently by the product owner. Deliberately **not** applied to any computed fare — every fare in the app is the token fare. The discount is surfaced as text only, in the ticket guidance card's note.
- **Operational facts:** Concessions, luggage limits, penalties, refund policy, ticket validity windows, and the Phase 1/Phase 2 token & CSC restriction were scraped from GMRC's official `fare-rules`, `smart-cards`, `national-common-mobility-card-ncmc`, and `train-information` pages and stored in `app/src/data/metroInfo.json`, with per-fact source quotes and any figure GMRC doesn't state left `null` rather than filled in from third-party sources. Re-read in full on 2026-07-28, adding `purchase` (where and how tickets are actually bought), `ticketConditions` (10 rules the earlier pass missed — screenshots of a QR are invalid, reverse-direction travel is not permitted, a dead phone is not an excuse), two overstepping penalties, and the two paper fare media. Its destination is now specified: the **Fares & ticket rules** page under YOU (§4.5.1), with the GO ticket card (§4.2) as the in-context excerpt.
  - One cross-source tension is recorded rather than resolved: the fare-rules and NCMC pages describe NCMC purely as a bank-issued product, while the MMI page says it is "available at every station". Both are kept, with the discrepancy noted in `cards.ncmc.alsoAtStationsNote`, because picking a winner would be us deciding which GMRC page is wrong.

### 5.5 Reported durations — ride time vs `totalMins`

A journey option carries two different notions of "how long", and the UI must not confuse them.

- **`totalMins`** is the platform wait **plus** the ride. It is the correct figure for "when will I be there", and it is what the arrival clock time is derived from.
- **`rideMinsOf(option)`** is the time from **boarding to arrival**. It excludes the *first* leg's wait — that is the platform wait, and it is already expressed as a countdown ("Leave in N") wherever it matters — but **includes every later leg's wait**, because interchange time is time you genuinely spend on the trip.

Anything labelled as the trip's own duration uses `rideMinsOf`, never `totalMins`. Doubling the platform wait into the duration is what made a plan opened at 4am bill an 8-minute hop as a **2h 17m journey**, in the largest type on the screen. The helper takes anything carrying `legs`, so it works on a `JourneyOption` or on the plan, and returns `null` when there are none.

### 5.6 Station & passenger reference data

Everything the app knows about stations as *places* and about riding the metro as an *activity*, scraped from GMRC's own pages on 2026-07-28. Three files, split by who consumes them:

| File | Shape | Consumed by |
|---|---|---|
| `app/src/data/stationFacilities.json` | `stations[id]` → `structure`, `gates[]`, `lifts[{lift, gates[]}]`, optional `multiModal` | Station Info tab (§4.4.1), arrival guidance (§4.2), search (below) |
| `app/src/data/passengerInfo.json` | `facilities`, `emergencyFacilities`, `dosAndDonts`, `prohibitedItems`, `contact`, `lostAndFound`, `social`, `officialApp`, `officialLinks` | YOU reference pages (§4.5.1) |
| `app/src/data/metroInfo.json` | fare, ticketing and card rules (extended 2026-07-28 with `purchase`, `ticketConditions`, two overstepping penalties, two paper fare media) | GO ticket card (§4.2), Fares & ticket rules page (§4.5.1) |

**Access.** `features/journey/stationFacilities.ts` mirrors the `stationImages.ts` pattern: `stationFacilities(id)` returns the record or `null`, plus `accessibleGates()`, `formatGateList()` and `stationsWithMode()`. Typed at the accessor, JSON imported directly, per the repo convention.

**Sourcing rule — the same one as `fares.json` and `stations.json`.** Only facts GMRC states appear. Nothing is filled in from a wiki, a blog or a transit aggregator, and every file carries a `_meta.rule` saying so with per-fact source URLs. Two consequences worth stating plainly, because both look like bugs otherwise:
- **Never published per station:** toilets, Wi-Fi, ATMs, feeder-bus *routes*, and **gate landmarks** — the gate table numbers gates but never says which road each opens onto. Gate 3 is therefore "Gate 3", not "Gate 3 (Ashram Road)". A landmark per gate is the single highest-value missing field in this dataset; it would need a survey, not a scrape.
- **`multiModal` is on 10 of 53 stations.** Its absence means "GMRC lists no *built* interchange here", never "no buses nearby", and the UI must not render it as a negative (§4.4.1).

**Regeneration.** `node scripts/fetch-station-facilities.mjs` re-scrapes both source pages and rebuilds `stationFacilities.json`. It is defensive by design, because a silent mis-scrape here attaches one station's entrances to another: it throws if either page's table count changes, warns on any station name it can't map (both pages name stations differently — "Vadaj Metro Station" vs "Vadaj", "Kalupur Rly. Station"), re-validates the two Phase-2 facts that exist only as prose and an image caption, and **cross-checks every Entry-Exit cited by the MMI page against that station's operational gates**. All 10 currently pass, which is independent confirmation the cross-page name mapping is right. `passengerInfo.json` is hand-maintained: two of its sources are poster JPEGs with no machine-readable text.

**Staleness.** GMRC edits these pages — gates open, MMI extends with Phase 2A/2B. `_meta.scrapedOn` is surfaced in About & Data (§4.5.1) and re-running the script belongs on the release checklist. `_meta.notCovered` currently lists `sabarmati-railway-station` and should shrink to empty when it opens.

**Bundle & offline.** ~44 KB raw across the three files, bundled and precached like the rest of the transit data (§5.3), so every reference page works in airplane mode — which is the point, since the concourse of an underground station is exactly where you have no signal and want to know which gate has the lift. `stationFacilities.json` sits in the main bundle (the station sheet is boot-critical); `passengerInfo.json` is dynamically imported by the `/you/:topic` route (§4.6), since reference prose has no business in the boot path.

**What this data does *not* unblock.** §5.1's flat 3-minute interchange buffer still needs a per-station walking matrix. Knowing a station is elevated or underground does not supply one, and in any case all three interchanges (Old High Court, Motera Stadium, GNLU) are elevated, so `structure` cannot even discriminate between them. That item stays open.

### 5.7 User accounts & sync — Dexie + Supabase

Four kinds of user data exist: **saved stations** (favourite station ids), **saved journeys** (a named origin→destination pair), **recent trips** (last 5, evicted), and **preferences** (currently just theme). All four are small, all four are per-user, and all four are worth keeping when a rider changes phone. That last clause is the whole reason this section exists.

**Storage: Dexie owns all four.** One database, `metrothi`, tables keyed by a stable string id (`stationId`, `key`, `key`, `name` respectively) plus `syncOutbox`. Every record carries `updatedAt` (epoch ms) and a `deletedAt` tombstone.

**Deletes are tombstones, not row removals.** A deleted row that is *gone* locally is indistinguishable from a row the server has and we haven't seen yet, so a pull would resurrect everything you ever unfavourited. Rows are marked `deletedAt` and filtered out of reads; the tombstone is what syncs. They are only truly dropped when the server has acknowledged them.

**First paint must not wait on IndexedDB.** This is the real cost the §5.2 reversal accepted, and it is only sharp for **theme**: an async read means a frame of the wrong theme, which is a visible flash on every cold start. So theme is written to Dexie *and* mirrored to one `localStorage` key that exists purely as a synchronous paint hint, read before React mounts. Dexie stays the source of truth and the thing that syncs; the mirror is a cache that is allowed to be stale for exactly one frame. The three list tables have no such problem — they render empty-then-populated, which is what they already did behind a `useEffect`.

**Sync: last-write-wins per row, on `updatedAt`.** Deliberately not CRDTs and not per-field merge. The data is a set of flags and a short list; the realistic conflict is "favourited on my phone, unfavourited on my laptop", where the later action is the one the rider meant. LWW is the honest fit, and per-row granularity means two different stations favourited on two devices both survive — the conflict is per station, not per table.

**The outbox is what makes it offline-correct.** A local mutation appends an intent to `syncOutbox` in the *same Dexie transaction* as the data write, so a write cannot be persisted-but-unqueued. A drain runs on sign-in, on reconnect, and after each mutation; it pushes, then pulls rows changed since the last cursor, then reconciles by `updatedAt`. Every step is idempotent — the push is an upsert on `(user_id, id)` — because the one guarantee an offline queue cannot make is "delivered exactly once". A failed drain leaves the outbox intact and retries; it never drops a write to make progress.

**Auth: Supabase GoTrue, email/password in this pass.** Google/Apple are in scope (§3) but each needs OAuth credentials configured in a console the app cannot reach, so they are wired as a documented follow-up rather than half-built. Sign-out clears the Supabase session and **keeps local data** — signing out of a transit app should not delete your favourite stations.

**Server-side: RLS is the security boundary, not the client.** Four tables, each with `user_id uuid references auth.users`, each with row-level security enabled and policies restricting every operation to `auth.uid() = user_id` (§3). The client ships the anon/publishable key, which is public by design; nothing but RLS stops one user reading another's rows. The `service_role` key must never appear in the app bundle or the repo.

**Anonymous data on first sign-in is merged, not overwritten.** A rider who used the app for a month then signed in has local rows with no `user_id`. Those are pushed as theirs, then reconciled against whatever the server holds by the same LWW rule. Discarding them would punish exactly the local-first behaviour the app encourages.

### 5.8 Product analytics — a second, one-way outbox

*This narrows §5.2's "PostHog (Product analytics with stringent PII stripping)". Sentry for error tracking stands unchanged and is a separate concern. The PostHog half is replaced by a `public.events` table on the Supabase project that already exists, for one reason the hosted alternative cannot answer: **this is a metro app, and its riders are underground.** Every hosted SDK — PostHog, Plausible, GA — fires a beacon and drops it when the network is gone, so the sessions most worth measuring are precisely the ones that never arrive, and every number is quietly low by an unknown amount that correlates with the thing being measured. §5.7 already built a durable queue for exactly this failure. The second reason is smaller but real: a 50 KB SDK on a boot path this section spends whole paragraphs defending is a bad trade for a solo project's first analytics.*

**Traffic and Web Vitals are not this.** Pageviews, referrers, devices and real-user Core Web Vitals come from Vercel Web Analytics and Speed Insights — ~1 KB each, cookieless, no dashboard to build, and Speed Insights measures the §7.1 criterion on real riders instead of on a lab run. That covers "how many people". §5.8 covers "did the app work for them", which is the half that can fail silently.

**One table, append-only, and it is not a sync table.** `events` is a fifth Dexie table and a fifth Postgres table, deliberately outside `SyncTable` and outside `syncOutbox`. It has no tombstone, no pull, no cursor, and no last-write-wins, because none of those mean anything for a fact that already happened. Feeding it through `syncEngine.ts` would put rows in a per-row LWW reconciler that has no conflict to resolve, and would break the moment it ran signed-out — which is the common case.

**The queue is at-least-once, so the server dedupes.** Same guarantee and same limits as §5.7: a drain that fails leaves the queue intact and retries, and never drops an event to make progress. The client generates each row's `id` as a uuid at the moment the event fires, and the insert is `on conflict (id) do nothing` — so a batch that was delivered but whose acknowledgement was lost costs a no-op instead of a duplicate. **The local queue is capped** (500 rows, oldest evicted): a rider offline for a week must not grow IndexedDB without bound, and old events are the ones worth losing.

**Two timestamps, because the client clock is not evidence.** `at` is epoch ms from the browser and is the only thing that preserves ordering within a device — the journey was planned at 09:41 on a platform and the row arrived at 09:58 above ground. `received_at` is `now()` on the server and is the only one that is monotonic, trustworthy, and safe to build a time series on. Keeping one would mean either believing every device's clock or losing offline ordering. Neither is acceptable, so both are stored and each is used for what it can answer.

**There is no `user_id` on this table, and that is the design.** §5.7's four tables are user data the rider owns and takes to a new phone. Events are not that. Joining behaviour to `auth.uid()` would build a named commute log for the minority of riders who signed in, in exchange for analysis that a random `device_id` answers just as well. The `device_id` is a uuid in Dexie `meta`, never derived from anything, never sent to the sync tables, and resettable from YOU.

**What may be recorded is an allowlist, enforced in Postgres, not a convention.** `name` is constrained by a `check` to a fixed set, so adding an event is a migration someone reads rather than a string someone types. Station ids are the bundled 53 — a low-cardinality enum, safe to store in their own indexed columns. **What is never recorded: GPS coordinates, place-search query text, resolved POI names, any free text at all, and anything traceable to `auth.uid()`.** The §7.4 search criterion is measured as a boolean — did the query resolve to a station — because "GIFT City Club" typed at 08:30 on a stable device id identifies a person in a way a station pair does not.

**Raw rows are purged at 90 days; rollups are kept.** A stable device id plus origin→destination pairs, retained forever, is a commute history — and India's DPDP Act treats that as personal data whether or not a name is attached. The purge is the thing that makes a stable device id defensible instead of merely convenient. Aggregates survive it, and aggregates are what the dashboard reads.

**The client can insert and cannot read.** RLS is on, the table has exactly one policy — insert, `with check (true)`, for `anon` and `authenticated` — and no select, update or delete policy at all. With RLS enabled and no policy, PostgREST returns zero rows to both roles, so the public anon key buys the ability to append and nothing else. **The same rule that governs §5.7's four tables applies here in reverse: the exception is the point, and a `select` policy added later silently turns every rider's queue into every rider's readable history.** The dashboard reads through views declared `security_invoker`, because a view over an RLS table defaults to running as its owner and would hand back exactly the rows the missing policy was there to withhold.

**Opt-out is a `prefs` row, so it syncs.** Analytics off means `track()` returns before touching Dexie — not queued-and-discarded. It sits in YOU beside the other preferences and, being a §5.7 pref, follows the rider to their next device.

**The event list is written against §7, not against curiosity.** `plan_impossible` and `fare_unavailable` exist because §7.3 and §7.5 are accuracy claims that can only be falsified in the field: a route that shows a completable ETA after the last train has left is a data bug no test caught, and the app cannot tell you it happened. That is the return on this section — the rest is counting.

---

## 6. Localization & Multilingual Support (Planned)

**Goal:** A full app-wide language switch between **English (`en`, default), Hindi (`hi`), and Gujarati (`gu`)**, selectable from the YOU screen and persisted across sessions. All three are left-to-right, so **no RTL layout mirroring is required**.

The work splits into two very different surfaces. The UI framework is the small, unblocked part; the translated *content* — especially the 54 station names — is the larger part and gates the feature feeling complete.

### 6.1 Library & architecture
- **`react-i18next` + `i18next`** (chosen over a hand-rolled context because Hindi and Gujarati have real CLDR plural rules, and the app already has interpolated strings — `Starts in {duration}`, `Stop {n} of {total}` — that i18next handles natively). A custom context mirroring `ThemeContext` was considered and rejected on the plural/interpolation cost.
- `app/src/i18n/index.ts` initialises i18next: registers the three resource bundles, `fallbackLng: 'en'`, `interpolation.escapeValue: false` (React already escapes), and reads the persisted language on boot.
- A thin `useLanguage()` hook wraps `i18n.changeLanguage` + persistence, **mirroring the existing `ThemeContext` pattern** (`contexts/ThemeContext.tsx`): the choice is written to `localStorage['metrothi-lang']` and reflected onto `document.documentElement.lang`, exactly as the theme flow writes `data-theme`.

**As shipped, the storage half is a `prefs` row *plus* that mirror, not the mirror alone** — §5.7 landed between this section being written and phase 1 being built, and language is a preference like walking pace, so it syncs. Dexie is the source of truth; `metrothi-lang` is the first-paint cache, held to the same rule as theme's: allowed to be one frame stale, never the thing that syncs. Three consequences worth recording:

- **No inline script in `index.html`**, unlike theme. Theme paints the document background before React exists, so it has to resolve earlier than any module can run. Language only affects rendered text, and there is no text until `main.tsx` runs — so reading the hint at module scope is early enough, and the pre-paint script stays as short as it is.
- **No provider.** i18next already holds the language globally and `useTranslation` already re-renders every consumer, so a `ThemeContext`-shaped provider would be a second source of truth for one value. Instead `LanguageSync` — a leaf that renders nothing, mounted beside `ScrollReset` for the same reason: its `useLiveQuery` must not re-render the route tree, map included, for a value the shell only forwards.
- **No write-back guard**, unlike `ThemeContext`. That one holds React state which both the hydrating read and the toggle set, so a cold start could restamp `updatedAt` and beat a genuinely newer preference under last-write-wins. This reads and never writes; the setter is the only writer.

**Measured cost, and it is the largest single fact about this phase.** `i18next` + `react-i18next` add **21.2 KB gzip (64.1 KB raw)** to the boot payload, summed across every file `index.html` loads: 210,397 → 231,624 gzip. That is four times what taking `/you` off the boot path bought back (§5.6), for a library chosen here on a plural-and-interpolation argument that the shipped string set barely exercises — Hindi and Gujarati use the same wording at `one` and `other` for both of the app's plurals, and interpolation is placeholder substitution. The choice stands because this document made it and the machinery is correct; the number is logged in `DISCREPANCIES.md` so it can be revisited against a hand-rolled resolver with the measurement in hand rather than without it.

### 6.2 Two translation surfaces
1. **UI chrome** — button labels, section headers, and status text (e.g. "Nearest Station", "Service ended", "Plan route") hardcoded across the ~24 `.tsx` components. Extracted into namespaced bundles `app/src/i18n/locales/{en,hi,gu}.json` and rendered via `t('namespace.key')`. Interpolated and pluralised strings use i18next placeholders and `_one`/`_other` suffixes.
2. **Data content** — station names (×54), line names, and landmark/place names. Handled as data, not UI strings:
   - Extend `app/src/data/stations.json` with `nameHi` / `nameGu` per station.
   - Extend `LINE_NAMES` in `app/src/features/journey/constants.ts` (e.g. Blue Line → नीली लाइन / વાદળી લાઇન).
   - A resolver (e.g. `stationName(station, lang)` in `journeyEngine.ts`) returns the localized name with **per-name fallback to English** when a translation is absent; every `station.name` read routes through it.

**The rule phase 2 turned out to be about, which §6.2 did not anticipate: a React-free module may decide *which* sentence, never *what* it says.** Four modules composed finished English inside logic the components only rendered — `liveStatus.ts` ("Board toward X"), `exitGuidance.ts` ("Step-free exit at Gates 2, 3 & 5"), `preferences.ts` ("Normal (5 km/h)") and `provenance.ts` ("Effective 18.05.2026 · hand-transcribed from GMRC"). Each is React-free on purpose and must stay i18next-free with it, so each now returns a **key plus the proper nouns to interpolate** (`{ key: 'live.boardToward', values: { heading } }`) and the one caller renders it. The decision stays in one tested place, the wording moves to the bundles, and the tests assert the *data* — gate numbers, a preset, a date — instead of an English string, which is what they should have asserted anyway.

**Two shapes are banned outright, both learned from bugs rather than from theory.** A sentence built by appending a translated fragment to a translated sentence: English tolerates it, Hindi and Gujarati put the verb last, so the appended clause lands mid-sentence. Where a line has optional parts it gets **one whole key per shape** — `live.walkTo` / `walkToThenWait` / `walkDurationTo` / `walkDurationToThenWait` is four keys for four shapes, on purpose. And behaviour read back out of a rendered string, which is what broke `StationInput` (§6.6's 2026-07-31 entry); `LineStatusPills` derives a short label from `LINE_NAMES` and is safe **only** because line names are English in every language (§6.8), which is worth a comment at the call site rather than a coincidence.

### 6.3 Dynamic formatting
Durations, dates, and distances (`formatDuration`, "Effective 18.05.2026", "600m ago" in `journeyEngine.ts`) route through `Intl` (`Intl.NumberFormat` / `Intl.DateTimeFormat`) or i18next formatters. **Decision pending:** keep Western digits (recommended — matches GMRC signage) vs. Indic numerals.

### 6.4 Fonts (easily missed)
The app currently loads **only Space Grotesk** (`app/index.html`), which has **zero Devanagari or Gujarati glyph coverage** — without action, Hindi/Gujarati would render as tofu. Add **Noto Sans Devanagari** and **Noto Sans Gujarati**, and switch `font-family` by active language via a `[lang="hi"]` / `[lang="gu"]` rule in `app/src/index.css` (the `<html lang>` attribute is already set by `useLanguage()`).

**Three deviations from that as built, each measured:**

- **The families are not in `index.html`'s `<link>`.** Adding them takes that render-blocking stylesheet from 546 B to 2,008 B gzip, which every English rider pays for a script they cannot see. Each family is its own stylesheet instead, injected at i18n-init time for the language that needs it — 569 B gzip for Devanagari, 1,563 B for Gujarati, nothing for English. Verified live: a cold start in Gujarati injects the `gu` link only.
- **The switch goes through a `--font-app` token, not a `[lang]` `font-family` rule.** Two screens set `fontFamily` in an inline style, which no CSS rule can override, so the selector-only approach would have silently done nothing on both. Same reason the colours are tokens.
- **Space Grotesk stays *first* in every language**, with Noto behind it. Font fallback is per glyph, so Latin — which every station name still is (§6.8) — keeps the app's typeface, and only Devanagari and Gujarati characters reach Noto. Putting Noto first would have re-set all 54 station names in a different face.

**Offline caveat.** `vite.config.ts` runtime-caches Google Fonts, so a language you have used before works in airplane mode like the rest of the app. A *first* switch to Hindi or Gujarati while offline gets the translations without the glyphs. Precaching both families for every install would cost every rider ~100 KB of fonts to insure a first switch most will never make.

### 6.5 Settings UI
A **Language** section in `YouScreen.tsx` — a three-way segmented selector (`English` / `हिंदी` / `ગુજરાતી`, each label in its own script so it is self-identifying), styled like the existing `Row` + `ThemeToggle`, calling the `useLanguage()` setter.

### 6.6 Phased implementation plan
| Phase | Work | Blocked? |
|---|---|---|
| **1 — Infra** ✅ *shipped 2026-07-31* | Deps installed; `i18n/index.ts` init with three inline bundles; `useLanguage()` + `LanguageSync`; language is a `prefs` row with a first-paint mirror (§6.1). | No |
| **2 — UI extraction** ✅ *completed 2026-08-02* | Every screen is on `t()`. The 2026-07-31 four (`HomeScreen`, `YouScreen`, `Planner`, `StationInput`) plus `StationDetail`, `LiveJourneyScreen`, `TrainRouteSheet`, `HomeSearch`, `SearchBar`, `LineStatusPills`, `LocationNotice`, `Countdown`, `DepartureRow`, `ExitGuidance`, `ErrorBoundary`, `InfoPage`'s chrome, the four journey-sheet components, the station-sheet pair, and YOU's own sub-sections (`AccountCard`, `PreferencesSection`, `SavedData`, About & Data). **Four React-free modules hand back keys instead of sentences** rather than importing i18next: `liveStatus.ts`, `exitGuidance.ts`, `preferences.ts`, `provenance.ts` (§6.2). | No |
| **3 — Name data** | Add `nameHi`/`nameGu` to `stations.json` and `LINE_NAMES`; add the resolver with English fallback. | **Yes — needs an official GMRC name source (see §6.8)** |
| **4 — Formatting** | Localize durations/dates/distances via `Intl`; resolve the numeral-system decision. | No |
| **5 — Fonts** ✅ *shipped 2026-07-31* | Noto Devanagari + Gujarati, injected per language; `--font-app` token (§6.4). | No |
| **6 — Settings UI** ✅ *shipped 2026-07-31* | Three-way segmented picker in `YouScreen.tsx`, each label in its own script. | No |
| **7 — QA** | Per-language pass: no missing-key warnings, no tofu, no overflow (Indic strings run longer than English — check the collapsed sheet peek and buttons), correct `<html lang>`, persistence across reload. | Done for every screen phase 2 touched — see the 2026-08-02 change-log entry for the method |

**Critical path:** Phases 1, 2, 5, 6 and 7 are shipped, and every screen a rider can reach is translated. **Phase 4 is now the whole of what is left that isn't blocked**, and it is what a Hindi rider notices first — see below. Phase 3 remains the only hard dependency (§6.8).

**Phase 4 is what a Hindi rider notices first, and it is bigger than it looks.** `formatDuration` lives in `journeyEngine.ts`, which is React-free by design and has twelve UI call sites, so every duration still reads "12 min" and "1h 05m" in every language; the planner's date pill still formats in the *browser's* locale; and the engine composes its own English warning sentences ("Blue Line hasn't started service yet — first train in …"), which §6.2 never accounted for as a surface. Localising the first without forking a second formatter, and the third without importing i18next into the engine, is the real content of that phase.

### 6.7 Third surface — scraped GMRC reference content

§6.2 splits translation into UI chrome and name data. The reference content added in §5.6 is a **third surface** with different rules again, and it is the largest body of prose in the app: ~30 rules and ~30 list items of official, partly legal text (penalties, the Metro Rail (O&M) Act 2002 prohibited-items list, ticket conditions).

**We must not translate it ourselves.** Machine- or hand-translating GMRC's penalty and prohibited-items wording into Hindi and Gujarati would put words in a transport authority's mouth on matters where a rider could be fined, and it fails the same test §6.8 applies to station names.

**We don't have to.** GMRC's own Do's & Don'ts and Prohibited Items posters are **trilingual** — Gujarati, Hindi and English side by side in the same image, already transcribed for English in `passengerInfo.json`. So for those two topics the authoritative translations exist at the same source and need only the same transcription pass, which makes them **unblocked**, unlike the station names of §6.8.

Per-topic disposition:

| Topic | Hi/Gu source | Status |
|---|---|---|
| Do's & don'ts, Prohibited items | The same GMRC posters, other two columns | **Unblocked** — transcribe alongside the English pass |
| Fares & ticket rules, Facilities, Safety & emergency | English-only on gujaratmetrorail.com | **Blocked** — falls back to English with an explicit "Available in English only from GMRC" note, rather than a silent untranslated block |
| Contact / lost & found / links | Phone numbers, emails, URLs | Not translatable content; only the surrounding labels are UI chrome (§6.2) |

Structurally this means `passengerInfo.json` gains parallel `*_hi` / `*_gu` arrays for the two poster topics only, resolved with per-string English fallback — the same resolver shape as §6.2's station names, not a second mechanism.

**Until that transcription happens, the YOU screen says so rather than leaving it to be inferred.** The topic rows' own titles and blurbs (`features/info/catalog.ts`) stay English in every language — translating a row that opens an English page promises more than the page delivers — and a line under each of the two reference sections reads "These pages are available in English only for now" whenever the app is not in English. That wording is deliberately a statement about *Metrothi*, not about GMRC: the Do's & Don'ts and Prohibited Items posters are trilingual at source, so "GMRC publishes these in English only" would be false.

### 6.8 Open dependency — station & place name source
Hindi/Gujarati station, line, and landmark names **do not exist anywhere in the repo today.** GMRC station signage is trilingual, so authoritative names exist, but they must not be machine-transliterated and shipped unverified (54 proper nouns × 2 languages, plus landmarks). **Required input:** a table keyed on the existing `id` slugs in `stations.json` with Hindi + Gujarati columns (+ line and place names). Until it lands, Phase 3 stays stubbed and the UI falls back to English per name.

---

## 7. Success Criteria for Production Launch

1. **Performance & Installability:** App is installable per Chrome's current criteria (web app manifest + registered service worker, verified in DevTools → Application), and scores well on time-to-interactive. *Note: the standalone "PWA" Lighthouse category was removed in Lighthouse v12 (GoogleChrome/lighthouse#15535) and no longer exists — the original "100 Lighthouse PWA score" target is unmeasurable. Track Lighthouse **Performance** ≥ 90 for the TTI half instead.*
2. **Offline Resilience:** A user in airplane mode can successfully plan a multi-transfer journey and view the exact fare and time estimate.
3. **Data Accuracy:** Zero cases where an impossible trip (e.g. arriving after the last train leaves a transfer station) shows a completable ETA.
4. **Place Resolution:** Users can search for "GIFT City Club" or "Narendra Modi Stadium" and the app correctly routes them to the nearest metro station (GNLU / Motera) with a walking ETA tail.
5. **Fare Accuracy:** The fare shown for a trip matches GMRC's own published fare for that trip (see §5.4), never a fabricated or guessed number — if a route's distance can't be resolved, the app shows nothing rather than an incorrect figure.
6. **Station Fact Accuracy:** Every physical claim the app makes about a station — gate numbers, lift locations, structure, onward connections — traces to a GMRC page (§5.6). No amenity is invented, inferred, or imported from a third party, and no absence is rendered as a negative ("no lift here", "no connections"). Sending someone to a gate that isn't open is a worse failure than saying nothing, in the same way a photo of the wrong station is (§4.6).

---

## 8. Delivery plan — station & passenger information

Ordered by rider value per unit of work. Phases 1–3 are unblocked and independent; nothing here depends on auth, sync, or the map.

| Phase | Work | Surfaces | Blocked? |
|---|---|---|---|
| **1 — Station Info tab** ✅ *shipped 2026-07-28* | Entrances, step-free access and Connections blocks; `structure` tile; update the stale `StationInfoPanel` doc-comment (§4.4.1). | `StationDetail.tsx` | No |
| **2 — Reference pages** ✅ *shipped 2026-07-29* | `InfoPage` + topic registry (§4.6); the nine rows of §4.5.1 wired into two new YOU sections as **eight pages plus one direct store link**; `tel:`/`mailto:` actions; About & Data provenance read from `_meta`; `/you/:topic` routes with dynamic import. | `YouScreen.tsx`, new `features/info/` | No |
| **3 — Last-mile guidance** ✅ *shipped 2026-07-29* | Destination exit gate + onward connection on the results screen's destination row and on the Live Journey's final stop (§4.2). | `RouteTimeline`, `LiveJourneyScreen`, new `exitGuidance.ts` | No |
| **4 — Discovery** ✅ *shipped 2026-07-29* | `multiModal.modes` as search keyword aliases on both search surfaces; connection facet in the STATIONS directory; modes in every station row's meta (§4.4). | `fuzzySearch.ts`, `HomeSearch.tsx`, `Planner.tsx`, `stationFacilities.ts` | No |
| **5 — Chip row** ✅ *shipped 2026-07-29* | Connection chips in the station sheet, one per mode. Measured at 375px before shipping and **accepted**: the gate's premise (a wrap hides the row) no longer holds, and the measured cost is 30px of map on one station in the common state (§4.4.1). | `HomeScreen.tsx` | No — was gated on measurement |
| **6 — Ticket card depth** ✅ *shipped 2026-07-29* | Where to get an NCMC folded into the GO ticket guidance card, on the cross-phase branch only (§4.2). Not `metroInfo.purchase` as written — that section is about buying *tickets*, and the trips this card constrains need a *card*. | `JourneySummary`, `journeyEngine` | No |
| **7 — Localization** | Transcribe the Gujarati/Hindi columns of the two GMRC posters; English-only note on the remaining topics (§6.7). | `passengerInfo.json` | Only by §6 shipping first |

**Critical path:** Phases 1–6 are shipped. Only **7 — localization** remains, and it waits on §6.

### 8.1 Delivery plan — accounts & sync

Separate from the information phases above: this one is about user data, not GMRC data.

| Phase | Work | Surfaces | Blocked? |
|---|---|---|---|
| **A — Dexie store** ✅ *shipped 2026-07-30* | `data/db.ts`: four tables + outbox + tombstones; one-time `localStorage` import; all six read/write sites moved onto `useLiveQuery` (§5.7). Reverses the §5.2 "no Dexie" decision. | `db.ts`, `useSavedStations`, `ThemeContext`, `App`, `Planner`, `HomeSearch`, `LiveJourneyScreen` | No |
| **B — Supabase auth & sync** ✅ *shipped 2026-07-30* | Lazily-loaded client, email/password auth, `syncEngine.ts` push/pull with per-row LWW, `supabase/schema.sql` with RLS, account + Data & sync UI (§4.5). | `services/supabase.ts`, `services/syncEngine.ts`, `contexts/AuthContext.tsx`, `features/account/` | No |
| **C — Google / Apple sign-in** | `signInWithOAuth` for both providers; `detectSessionInUrl` back on, plus a redirect route. | `AuthContext`, `AccountCard` | **Yes** — needs OAuth client credentials configured in the Google and Apple consoles, which is a project-owner action, not a code change |
| **D — Surface the synced data in YOU** ✅ *shipped 2026-07-31* | Saved places, Saved journeys and Past trips read their tables through `useLiveQuery` and expand to the full list, each entry removable. `SavedData.tsx`. | `YouScreen`, new `SavedData.tsx` | No |
| **E — Preferences worth syncing** ✅ *shipped 2026-07-31* | Walking speed (three presets) and default departure station, both `prefs` rows. The pace reaches the engine as `PlanConfig.walkSpeedKmh`; the station outranks GPS in the planner's auto-fill. `data/preferences.ts`, `usePreferences.ts`, `PreferencesSection.tsx`. | `journeyEngine`, `Planner`, `HomeScreen`, `App`, `YouScreen` | Language still waits on §6 |

**Critical path:** Phases A, B, D and E are shipped. **C — Google / Apple sign-in** is the only one left, and it stays blocked on console credentials this repo can't reach.

**Not planned, and why:** a per-field merge or CRDT for conflicts. The data is a set of flags and a five-item list, so per-row last-write-wins is the honest fit (§5.7); anything cleverer would be complexity with no conflict to resolve.

**Not planned, and why:** a per-gate landmark ("Gate 3 — Ashram Road side") would be the single most useful addition to the Station Info tab and is the one thing GMRC does not publish (§5.6). It needs a physical survey of 53 stations. Until someone does that survey it stays out — inventing it from map data would fail §7.6.

### 8.2 Delivery plan — analytics

| Phase | Work | Surfaces | Blocked? |
|---|---|---|---|
| **A — Traffic & Web Vitals** ✅ *shipped 2026-08-02* | `@vercel/analytics` + `@vercel/speed-insights` behind one `components/Insights.tsx`, mounted as a leaf inside `BrowserRouter`. Route attribution via `matchPath` over the four declared patterns, and a `beforeSend` that strips query strings. **+1,575 bytes gzip** on the boot path, no new boot file. | `App.tsx`, `components/Insights.tsx`, `package.json` | No |
| **B — Server schema** | `supabase/analytics.sql`: `events` table, the `name` allowlist, insert-only RLS, `security_invoker` views, 90-day purge. Runs standalone against the existing project. | `supabase/analytics.sql` | No — but needs one run in the SQL editor, same as `schema.sql` |
| **C — Client queue** ✅ *shipped 2026-08-02* | Dexie v2 adds `events` (outside `SyncTable`); `services/analytics.ts` with a synchronous no-op `track()`, a 500-row cap evicting oldest, and a batched `on conflict do nothing` drain on `online` / `visibilitychange` → hidden / 5-min interval. `device_id` stamped from `meta` at drain time, not at track time, so `track()` never awaits IndexedDB. **+745 bytes gzip** on the boot path. | `data/db.ts`, `services/analytics.ts`, `data/preferences.ts`, `App.tsx` | No |
| **D — Call sites** ✅ *shipped 2026-08-02* | All **twelve** allowlisted events wired (the row previously said eleven — `install_prompted` and `installed` are two). The plan branch lives in `features/journey/planAnalytics.ts`, off the React tree, so `plan_impossible` is reachable from a test via `queryTime` instead of only by planning after the last train. **+698 bytes gzip.** | `App.tsx`, `planAnalytics.ts`, `StationDetail`, `HomeSearch`, `i18n/useLanguage`, `services/analytics.ts` | No |
| **E — Opt-out** ✅ *shipped 2026-08-02* | `analytics` pref row + a YOU toggle on a new shared `Switch` primitive. Off short-circuits before Dexie **and** clears whatever is already queued. `'on'` is written explicitly rather than deleting the row, so the choice syncs as a choice. Strings in all three bundles. | `preferences.ts`, `usePreferences.ts`, `PreferencesSection`, `settingsRows.tsx`, `i18n/locales/*` | No |
| **F — Dashboard** | Saved queries over the phase-B views in Supabase Studio. An in-app admin route is **not** planned: it would need an `is_admin` claim and a select policy, and §5.8 exists partly to not have one. | — | No |

**Critical path:** A is independent and can ship alone. B → C → D is the chain; E ships with D or the first events are collected without a way to decline. F is reading, not building.

**Not planned, and why:** session replay. It records the DOM, and this app's DOM is station names, a resolved destination, and sometimes a map centred on the rider — the three things §5.8 spends its length not collecting.

**Not planned, and why:** funnels keyed to a signed-in identity. Retention by account would be genuinely useful and is the one real cost of dropping `user_id` (§5.8). Paying for it means joining behaviour to `auth.uid()`, which is the exact trade this section declines.

---

## Change Log

- **2026-08-02** — **§8.2 phase E shipped, and `app_version` stopped being null on every row.** The opt-out is a `prefs` row with a YOU toggle, built on a new `Switch` in `settingsRows.tsx` rather than beside its one caller — that file's header states the rule, and a local copy in one screen is how two drift. `ThemeToggle` is deliberately **not** folded into it: it is a sun/moon three-part control with an icon either side, not a switch with a label, and collapsing them would mean a `variant` prop existing to serve two unrelated designs. **`'on'` is written explicitly rather than deleting the row**, so the choice syncs *as a choice* — a pref that is merely absent on the other device is indistinguishable from one that has not arrived yet, which is §5.7's tombstone argument in miniature. **The value line says what is not collected, not what is.** "Helps us improve the app" is the usual wording and gives a rider nothing to weigh; the three things they would actually worry about are their account, their location, and what they type into the search box, and none of the three is ever recorded (§5.8). Two whole keys, one per state, for the reason `WalkSpeedRow` uses two (§6.2). **Verified end to end in the browser, which is the only place this contract is real:** eight queued events went to zero the instant the switch flipped, `aria-checked` followed, the pref wrote `off`, it appeared in `syncOutbox` so it reaches the rider's other devices, and a station navigation while off queued **nothing**; flipping back on wrote `'on'` and events resumed. **`vite.config.ts` now reads `package.json` into `import.meta.env.VITE_APP_VERSION`**, confirmed as `"0.1.0"` on a real queued row — without it the `app_version` column was always null, which is the column that lets a spike in `plan_impossible` be told apart from the rollout that caused it. That surfaced a second source for the same fact: `YouScreen.tsx:300` hardcodes `v0.1.0-prototype` and will silently disagree the first time `package.json` is bumped. Logged in `DISCREPANCIES.md` rather than fixed inline, per the repo convention. Build clean, oxlint at its 7 pre-existing warnings, suite **424/424** with `locales.test.ts` confirming key parity across en/hi/gu.

- **2026-08-02** — **§8.2 phase D shipped: all twelve events wired, for +698 bytes gzip — and the plan branch moved off the React tree to be testable at all.** The row said eleven; `install_prompted` and `installed` are two, and that is corrected. **The one structural change is `features/journey/planAnalytics.ts`.** The feasible / impossible / fare-less branch was written inline in `App.tsx`'s `handlePlan`, which made it correct and unreachable: the only way to produce `plan_impossible` for a real rider is to plan at an hour when the last train has gone, and no amount of clicking a planner reproduces a wall-clock condition on demand. Moved out, it takes a `PlanResult` and returns which events to emit — the same shape as `liveStatus.ts` and `exitGuidance.ts`, which decide *which* thing and let the one caller act — and `queryTime` turns the condition into a parameter. Five tests now cover it against real engine output, including **an assertion that `rideMins` is `rideMinsOf(plan)` and specifically *not* `totalMins`** (§5.5), which fails if anyone "simplifies" it, and one that serialises the events from a `PlaceNode` plan and asserts the POI name is absent (§5.8). **`app_open` is guarded at module scope, not with a ref**, because the shell runs under `StrictMode` and every dev remount would otherwise inflate the denominator every other event is measured against — verified in the browser as exactly one row across a StrictMode double-mount. **The call-site coverage test had to be loosened, and the reason is worth recording.** It began as "every allowlisted name appears in a `track('…')` literal" and immediately failed the `planAnalytics.ts` refactor, because those three are now emitted through a variable — a real improvement it had no business blocking. It now asserts each name is referenced *outside its own declaration*, with the `EVENT_NAMES` array cut out of the corpus so the allowlist cannot satisfy an assertion about itself. Weaker, still catches the failure that matters, and mutation-checked both ways. **Verified live in the browser**, not only in tests: `app_open` once per launch, `station_viewed` one row per navigation with the right id (the apparent duplicate is StrictMode's initial-mount double-invoke, development-only), and `place_resolved` carrying `{ matched, queryLength }` and **no query text** — §7.4 measured as a boolean because the string a rider types is the most identifying thing in this app. Build clean, oxlint at its 7 pre-existing warnings, suite **424/424** (was 418).

- **2026-08-02** — **§8.2 phase C shipped: the client queue, for +745 bytes gzip.** Dexie goes to v2 with a fifth table, `events`, deliberately outside `SyncTable` and outside `syncOutbox` — an append-only fact has no tombstone, no pull and no last-write-wins, so routing it through `syncEngine.ts` would put it in a reconciler with no work to do and break it signed-out, which is the common case. **Three decisions are worth recording because each removed a problem rather than solving one.** `device_id` is stamped by the *drain*, not by `track()`: the id is a property of the device rather than of the moment, and reading it at track time would have meant an IndexedDB await on the boot path, which is the one thing §5.6 forbids. `track()` is therefore synchronous, total, and returns before touching Dexie — it sits on the line after the thing it measures, and analytics failing is never a reason for a journey to fail. And **eviction deletes rather than tombstones**, which is not a violation of §5.7's rule: a tombstone exists so a delete can be replayed against a server that may still hold the row, and this table never pulls, so tombstoning would keep the row forever under a different name and defeat the cap it was enforcing. **The opt-out has a cold-start window and it is handled explicitly.** `track()` cannot await a pref, so it treats analytics as enabled until hydration resolves — which means hydration *deletes* the queue when it finds the pref off rather than merely stopping later writes, and the drain re-reads the stored value rather than trusting the cache, so an opt-out made in another tab or synced from another device still stops the send. `readAnalyticsEnabled` treats **only** an explicit `'off'` as off, inverting this module's usual parse rule on purpose: everywhere else a corrupt value falling back to the default is right, but here it would make a failed read indistinguishable from a rider's explicit choice. **The test that earns its keep asserts the client's twelve event names against the `check` constraint in `supabase/analytics.sql`**, parsing the SQL rather than restating it — a name only the client knows is rejected by Postgres, and because events travel in batches one typo would block every event on the device forever. Mutation-checked: a single character changed in the client list fails it. Ten tests cover the rest of the silent failures — a failed insert keeping every row, a `23514` rejection dropping the batch so it cannot poison the queue, eviction taking the oldest rather than the newest, and `track()` not throwing against a closed database. **The v1 → v2 upgrade was verified in the browser against a database that already held rows**, not a fresh one: IDB version 20, all seven stores, `events` carrying its `at` index, and the pre-existing `meta` / `prefs` / `recentTrips` / `syncOutbox` rows intact. One toolchain note: the allowlist test reads the SQL through Vite's `?raw` rather than `node:fs`, because the app tsconfig's `types` is `["vite/client"]` — `readFileSync` and `__dirname` compile under vitest and then fail `tsc -b`, which is the actual typecheck gate. Build clean, oxlint at its 7 pre-existing warnings, suite **418/418** (was 408).

- **2026-08-02** — **§8.2 phase A shipped: traffic and real-user Web Vitals, for +1,575 bytes gzip on the boot path.** `@vercel/analytics` and `@vercel/speed-insights` behind one `components/Insights.tsx`, mounted as a leaf inside `BrowserRouter` for the reason `ScrollReset` and `LanguageSync` are — it reads the location to attribute a route and must not re-render the route tree, map included, to do it. Measured by summing every file `index.html` loads, per §5.6: 244,432 → 246,007 gzip, nine boot files before and after. **Two bugs were caught in the browser that neither the type-checker nor the test suite could see, and both were silent.** The first is a trap in `@vercel/analytics` worth writing down: passing `route` sets the script's `disableAutoTrack`, and the component's own pageview effect is guarded by `if (props.route && props.path)` — so `route` **without** `path` turns automatic tracking off and never turns manual tracking on. The build was clean, nothing logged, and the dashboard would simply have recorded zero pageviews forever. It was found by patching `window.va` and watching what a navigation actually emitted; it now sends `route: '/stations/:id'` with `path: '/stations/vastral-gam'`, which is also what stops 53 station pages being 53 dashboard rows instead of one route. Speed Insights has no such coupling and takes `route` alone. The second: both components register `beforeSend` from a `useEffect` keyed on the prop, so an **inline arrow re-registers on every render** — 2 re-registrations across 3 navigations, measured. That queues onto `window.vaq` until the script loads and drains it, and in this app the script may not load for a long time or at all, because the rider is underground. Hoisted to a module-scope generic, it registers once. **`beforeSend` strips query strings**, which strips nothing today — planning doesn't navigate (§4.2) and Photon is a `fetch` — and exists so the day someone adds `?dest=…` the rider's typed destination doesn't start flowing to a third party with no error and no review (§5.8). **No Workbox rule was needed**, contrary to the note this work started from: the service worker's only same-origin route is the `NavigationRoute`, which matches `mode: 'navigate'` and never sees a script request or a beacon — verified against the generated `sw.js`. Verified live across all four route patterns: `/`, `/stations/:id`, `/you`, `/you/:topic`, plus the unknown-topic redirect emitting its own pageview. Build clean, oxlint at its 7 pre-existing warnings with none in the new file, suite **408/408**.

- **2026-08-02** — **§5.8 and §8.2 added: analytics is specced, and it is not the PostHog line §5.2 has carried since the start.** Nothing is coded yet — this is the document-first half of the change. **The reason the hosted SDK loses is not bundle size, though 50 KB on this boot path would be reason enough.** It is that this is a metro app: every hosted SDK fires a beacon and drops it when the network is gone, so the sessions worth measuring are the ones underground, and every number comes back low by an amount that correlates with the thing being measured. §5.7 already built the durable queue that fixes it. So events go to a `public.events` table on the Supabase project that already exists, through a **second Dexie queue that is deliberately not `syncOutbox`** — an append-only fact has no tombstone, no pull and no last-write-wins, and running it through `syncEngine.ts` would put it in a per-row LWW reconciler with no conflict to resolve and break it signed-out, which is the common case. **The table has no `user_id` and one RLS policy.** Insert for `anon` and `authenticated`, `with check (true)`, and no select policy at all — with RLS on that returns zero rows to both roles, so the public anon key buys append and nothing else. `schema.sql`'s stated rule is four policies per table, so this lives in its own `supabase/analytics.sql` rather than under a header that would invite someone to "fix" it. **Two footguns are called out by name because neither errors:** a `select` policy added later turns every rider's queue into readable history, and a rollup view without `security_invoker` runs as its owner and bypasses RLS to hand back exactly what the missing policy withheld. **What may be recorded is a Postgres `check` constraint, not a convention** — twelve event names, station ids in their own columns, and never GPS coordinates, place-search query text, POI names, or free text of any kind; §7.4's search criterion is measured as a boolean because "GIFT City Club" at 08:30 on a stable device id identifies a person in a way a station pair does not. Raw rows purge at 90 days, which is what makes a stable `device_id` defensible rather than merely convenient under DPDP. **The return on all of it is two views:** `plan_impossible` and `fare_unavailable` are §7.3 and §7.5 failing in the field, and there is currently no way for the app to tell anyone that happened. Vercel Web Analytics and Speed Insights cover traffic and the §7.1 Web Vitals half separately, at ~1 KB and no dashboard to build. Session replay and identity-keyed retention are both explicitly declined, with the trade written down.

- **2026-08-02** — **§6 phase 2 is finished: every screen a rider can reach is now Hindi and Gujarati, not just the three that shipped first.** ~200 keys across `StationDetail`, `LiveJourneyScreen`, `TrainRouteSheet`, the four journey-sheet components, `HomeSearch`, `SearchBar`, `LineStatusPills`, `LocationNotice`, `DepartureRow`, `ExitGuidance`, `Countdown`, `ErrorBoundary`, and YOU's own sub-sections. Six things worth recording. **The real work was not the components — it was four React-free modules that composed finished English.** `liveStatus.ts`, `exitGuidance.ts`, `preferences.ts` and `provenance.ts` each decided a sentence *and wrote it*, inside logic that is deliberately free of React and must stay free of i18next; they now return a key plus its proper nouns and the one caller renders it (§6.2). That also improved their tests: `exitGuidanceFor` is asserted on gate **numbers** now rather than on "Step-free exit at Gates 2, 3 & 5", which was testing `en.json` through two layers. **`liveStatus` gained a test the component layer could never have had:** every one of its eleven state branches is enumerated and its emitted key looked up in `en.json`, because a typo there renders the literal string `live.boardToward` in the sheet header and nothing throws — and the two `APPROACHING_*` states are only reachable from a GPS proximity check, so a typo in either would have reached a rider before it reached CI. **Optional clauses get one whole key per shape, not concatenation.** `Walk to X`, `Walk 4 mins to X`, and both again with ", then wait up to N" are four separate keys: English tolerates appending, Hindi and Gujarati put the verb last, so an appended clause lands in the middle of the sentence it was appended to. **`SavedData`'s hand-rolled `plural(count, one, many)` is gone**, along with its hardcoded assumption that every language has two forms — and phase 2 added two plurals of a shape a hand-rolled resolver gets wrong, `journey.gateList` and `station.lineLabel`, which select on `count` while rendering something else entirely. That strengthens §6.1's library choice, and `locales.test.ts` grew a dedicated assertion for it. **One shadowing bug was found by translating, in the same class as `StationInput`'s:** `SavedData`'s `trips.map(t => …)` shadowed the translate function, so the first `t('…')` inside it would have been a runtime call on a trip object. **The measured cost is the headline, and it is bad.** The bundles went 15,930 raw to 61,126 — 3.8× — and the boot payload 234,304 → **247,551 gzip**, +13.2 KB on every cold start, three quarters of it two scripts the median rider cannot read. That is 2.5× what the whole library was flagged for and five times what taking `/you` off the boot path bought back. The condition `DISCREPANCIES.md` set for splitting `hi`/`gu` off the boot path — "the trade flips if the bundles grow" — is now met, and it is the largest single boot-path item with a known fix. Verified live at 375 and 320px in all three languages across home, station sheet, station page (both tabs), planner, journey summary, route timeline, live journey and the train-route sheet: zero unresolved keys (scanned every text node for a `namespace.key` shape), zero document overflow, no console errors, and the Gujarati Noto stylesheet injected only on the switch to `gu`. `tsc -b --noEmit` clean, oxlint at its 9 pre-existing warnings, suite **408/408** (was 390).

- **2026-07-31** — **§6 phases 1, 5 and 6 shipped, plus phase 2 on the three highest-visibility screens — the app has a working language switch.** English, हिंदी and ગુજરાતી, chosen from YOU, persisted as a `prefs` row and rendered by i18next. Seven things worth recording. **Language is a synced preference *and* a first-paint mirror**, which is theme's pattern applied to the second value that qualifies for it — but it needs **no inline script in `index.html`**, because theme paints the document background before React exists while language only affects text that doesn't exist until `main.tsx` runs (§6.1). It also needs no provider and no write-back guard: i18next already holds the language globally, and `LanguageSync` reads without ever writing, so the cold-start-beats-a-newer-device hazard that shaped `ThemeContext` cannot arise here. **The library costs 21.2 KB gzip on the boot path** — 210,397 → 231,624 across every file `index.html` loads, four times what taking `/you` off that path bought back — for plural machinery the shipped strings barely exercise, since Hindi and Gujarati use one wording for both plural forms. Kept because §6.1 chose it, logged in `DISCREPANCIES.md` so the choice can be re-argued against a number instead of a guess. **The two Indic bundles are 2.68 KB of that and ship statically anyway**, because lazy-loading them would trade ~1 KB for a frame of English on every Hindi cold start — the language equivalent of the theme flash §5.7 exists to prevent. **The fonts are not in the boot `<link>`** (§6.4): adding both families takes that render-blocking request from 546 B to 2,008 B gzip for every English rider, so each is its own stylesheet injected only for the language that needs it, and the switch runs through a new `--font-app` token because the two screens setting `fontFamily` inline could never have been overridden by a `[lang]` rule. Space Grotesk stays first in the stack in every language, so per-glyph fallback keeps all 54 English station names in the app's own face. **The reference pages stay English and now say so** (§6.7) rather than dressing a GMRC page in a Hindi label. **One real bug was found by translating, not by testing:** `StationInput` derived its placeholder from `label === "From"` and its clear button from `` `Clear ${label}` ``, so the moment the label was translated the *origin* field offered "Where to?" and the button announced "Clear થી" — neither throws, neither is visible in English. It takes a `field` discriminator now, and `locales.test.ts` guards the whole class: bundle parity, orphaned keys, dropped `{{placeholders}}`, and i18next's own plural resolution per language. Each of those four assertions was checked against a deliberately broken bundle before being trusted. **The most visible thing still missing is phase 4** — durations, dates and the engine's own composed warning sentences are English in every language; see §6.6.

- **2026-07-31** — **Two backlog items cleared: `/you` off the boot path, and eight animations that never ran.** Neither is a phase; both are things the delivery plan had logged and stepped around. **The settings screen is now a `lazy()` route**, like `/you/:topic` one level below it, with a fallback that draws its real furniture rather than a spinner. **The measurement the backlog carried for it was wrong, and the correction generalises:** it claimed 9.94 KB gzip, read off the build report's `index-*.js` line. Splitting a route also lifts shared modules — here `jsx-runtime` and `preload-helper` — out into their own files that `index.html` still loads at boot, so the entry chunk always drops further than the payload does. Summed across every boot file the real figure is **5.32 KB gzip (21.0 KB raw)**, still worth having and about half of what was advertised; §5.6's boot-path rules are unchanged but two doc-comments justifying themselves with "this module is in the boot bundle" were re-argued rather than deleted, since the prose they keep out would now land in the settings chunk instead of the main one. **The eight dead `animate-in` classes are gone** — Tailwind v4 here has no animate plugin, so they generated no CSS and the entrances simply never happened. Seven are framer-motion now, which is already the app's animation library and already did this exact job for the search suggestions; adding a CSS animation package would have put weight back on the boot path in the same change that took weight off it. **One was built and dropped after measuring:** `StationDetail`'s `slide-in-from-right-4` starts the page root 16px right of its box, and `<main>`'s `overflow-y: auto` makes its `overflow-x` compute to `auto`, so the entrance gave `main` a 16px horizontal scroll range at 375px — measured on the animation's own first frame. Only the fade survives. **One was deleted rather than restored:** `App.tsx`'s route-fade sits outside `<Routes>`, so it would fade the whole app in once at boot, and a genuine per-route transition needs `AnimatePresence` keyed on the path — which would unmount `HomeScreen` on every navigation and tear down the map and sheet §4.1 keeps mounted on purpose. **Recorded because it limits the verification:** the Browser pane composited no frames at all this session (0 rAF callbacks in 600 ms, `document.timeline.currentTime` 0, screenshots timing out on every route), so each entrance was checked at its *initial* frame — the one that can overflow — and none was watched running.

- **2026-07-31** — **§8.1 phases D & E shipped — the YOU screen stops claiming its own features are unbuilt.** Five rows carried a `Phase 4` badge: three whose data had been live and syncing since 2026-07-30, and two that printed their own hardcoded default as if it were a stored value. **The lists earn their place by what the other surfaces can't do.** Saved journeys and recent trips were already rendered on the search overlay and in the planner, so a second read-only list would have been duplication; what neither offers is *seeing everything stored and removing from it*, which is what a settings screen is for. Removal goes through `toggleSavedStation` / `toggleSavedJourney` / `removeRecentTrip`, so it tombstones and queues rather than deleting — verified in the browser, since a raw delete here would resurrect on the next pull and nothing would throw. **Walking pace is a plan input, not engine state.** `walkMinsForKm(km, speedKmh?)` and `PlanConfig.walkSpeedKmh` carry the preference in per call, the same way `queryTime` already did. A settable module-level speed inside `journeyEngine.ts` was the tempting alternative and is wrong twice: it makes the engine's output depend on *when* it was called rather than on what it was asked, and boot order would let a plan run before the Dexie read lands. The engine still imports nothing from the store — only `DEFAULT_WALK_SPEED_KMH` from a React-free constants module. **Three presets, not a number field.** The walk figure is derived from a straight-line distance to the station door, so offering 4.7 km/h would claim a precision the input doesn't have. 5 km/h stays the middle option and the default, so nobody's existing estimates move. **A default departure station is not a location fix, and the planner had to stop treating it as one.** It outranks GPS but not an explicit "From here", and where it fills the source field the "~500 m away · 6 min walk" line and the location-retry notice are both suppressed — they describe the *nearest* station, and printing them beside a station the rider chose would attach the wrong distance to it. **Every pref read parses defensively.** Prefs sync, so a value can arrive from a newer build that offers a pace this one doesn't; `readWalkSpeed` falls back to the default for anything not in the preset list, which makes an unset pref and a corrupt one behave identically. `preferences.test.ts` asserts exactly that, because none of those failures throw. **Cost recorded:** the boot chunk went 203.89 → 207.17 KB gzip. All of it is on `/you`, which is still a static route while `/you/:topic` below it is `lazy()` — logged in DISCREPANCIES.md rather than fixed here.

- **2026-07-30** — **§8.1 phases A & B shipped — Dexie and Supabase, and the recorded decision one of them reverses.** User data moved from `localStorage` to Dexie, and now syncs to Supabase for signed-in riders. Six things worth recording. **This reverses §5.2's "Dexie deliberately not used", and not for the reason that entry named.** Its stated revisit condition was "only if user data outgrows localStorage's ~5 MB" — the data is still kilobytes, so on its own terms the old decision still stood. What changed is that an offline write queue needs indexed reads, atomic transactions and tombstones, and `localStorage` offers none of the three: one JSON blob per key, read-modify-written on every mutation, losing a concurrent write. The half of that decision about the *transit graph* was correct and stands — Dexie holds no GMRC data. **The async-hydration cost the old entry warned about is real, and lands entirely on theme.** It's the one value read before React mounts, so an async read there is a visible flash of the wrong theme on every cold start. Theme therefore keeps a synchronous `localStorage` mirror as a *paint hint* while Dexie stays the source of truth — with a guard that the value loaded from Dexie is not written back, because a cold start bumping `updatedAt` would let boot order beat a genuinely newer preference from another device under LWW. **Deletes are tombstones, not row removals**, throughout: a row that is locally gone is indistinguishable from one the server has and we haven't pulled, so hard deletes would resurrect every station you ever unfavourited. Verified in the browser, which is where it matters — after unfavouriting a migrated station and reloading, the migration's second pass did not bring it back. **`useLiveQuery` deleted a hand-rolled event bus**: `useSavedStations` used to broadcast `metrothi-saved-stations-change` because `storage` only fires in *other* tabs, and that scheme's failure mode was a write forgetting to dispatch. Observing the table removes the possibility. **The SDK is lazily imported and never touches the boot path.** Measured: `@supabase/supabase-js` inlined cost 104 KB raw / 26 KB gzip on the main chunk, which for a map-first app is the same objection §5.6 raises about 12 KB of reference prose, at twice the weight. Behind a dynamic import the boot bundle is 203.89 KB gzip against a 203.47 KB no-Supabase baseline, and an *unconfigured* build never requests the chunk at all — confirmed in the browser. The import goes through a one-line local module (`createSupabaseClient.ts`) purely so the emitted chunk isn't named `dist-<hash>.js` after the package's own path. **RLS is the security boundary, not the client.** The anon key is public by design and ships in the bundle; `supabase/schema.sql` enables row-level security on all four tables with `auth.uid() = user_id` policies, `to authenticated`, and `with check` on update so a row can't be reassigned to another user. Google/Apple (§3) are deliberately absent rather than half-built — each needs console credentials this repo can't reach (§8.1 phase C).

- **2026-07-29** — **§8 phase 6 shipped — and the phase corrects a claim §4.5.1 was shipping.** The cross-phase ticket card now names where to get the card it says you need. Four things worth recording. **The phase's own data source was wrong as scoped:** §8 said "fold in `metroInfo.purchase`", but that section describes buying *tickets* — station windows (cash/UPI/POS) and the GMRC app — and the GMRC app sells QR tickets only, which are not valid across phases. The rider stuck at a Phase-1↔2 gate needs a **card**, so the fact that closes the gap is `cards.ncmc`, not `purchase`. The payBy list is deliberately not attached to the card: GMRC states it about tickets and never about the NCMC, and borrowing it would be exactly the §7.6 invention this phase caught elsewhere. **GMRC says it once**, on the MMI page's Phase-2 tab, while the fare-rules and NCMC pages describe NCMC as bank-issued only — so the wording is "GMRC lists NCMC cards as available at every station", keeping the hedge `metroInfo`'s own `alsoAtStationsNote` records rather than promoting one page to a promise. **It renders on the cross-phase branch only** (`where: string | null`, null when the note names no constraint), and on the after-last-train path too — that early return builds its own `PlanResult`, so it gets its own assertion. **A §7.6 violation found and fixed in shipped phase-2 content:** the Fares & ticket rules page said crossing phases needs "an NCMC card **or a QR ticket**". GMRC's quote restricts tokens and CSC and says nothing about QR; phase 2 read permission out of that silence, and it contradicted the planner, whose NCMC-only rule was confirmed against fare-rules on 2026-07-20. The page now matches the engine. Measured at 375×812 on a feasible cross-phase plan, per phase 5's rule: the second line wraps to two lines and costs **34px**, and Start Journey still lands at 775px of an 812px viewport — 37px of headroom, the tightest the card has been.

- **2026-07-29** — **§8 phase 5 shipped — the measurement it was gated on, and what the measurement found.** The station sheet's chip row now carries one chip per connected mode. The gate said "only if it survives a 375px check without pushing the row under the fold"; **that failure mode no longer exists**, because `DraggableSheet.pointsFor` grows the peek to `max(collapsedHeight, headerHeight)`, so a wrapped chip row makes the sheet taller instead of being cut off — confirmed live at the collapsed snap, chip row bottom 779px in an 812px viewport. The measurement therefore became "how much map does a wrap cost", answered per station in each of the row's four states (§4.4.1): a wrapped line costs 30px of peek, and **AEC is the only station that pays it in the common state** (line running, station not your nearest), because it is the only one with three modes. Two decisions: **one chip per mode, not a "BRTS +2" roll-up** — the mode's name is the whole value, and a roll-up would hide two of three names at precisely the station that has three — and **PDEU shows nothing**, its `multiModal` being parking, an amenity rather than a way onward, which is the same call phase 3 made for its exit guidance. Worth recording for the next person who measures this row: a probe element appended to `document.body` renders **outside the app root's font**, and measured every chip ~4px narrow — enough to flip Ranip's verdict. Probes belong inside the subtree they model, checked against a live row before being trusted.

- **2026-07-29** — **§8 phase 4 shipped.** The interchange data is now reachable by mode instead of only by station name: `multiModal.modes` feed `fuzzySearch` as non-displayed keyword aliases on both search surfaces, and a connection facet filters the STATIONS directory. Four decisions worth recording. **The ranking rule is a hard band, not a weight:** every alias tier (8/6/4) sits below the weakest name tier (10), so "bus" can add Vadaj but can never push it over a station actually called that — asserted in `fuzzySearch.test.ts`, since the failure mode is silent reordering rather than an error. **Aliases don't do subsequence matching** even though names do; "brs" is a subsequence of "BRTS", and matching that loosely would attach the interchange stations to every short query. **Every station row shows its modes in the meta line, filtered or not** — a row that matched no part of what you typed is noise until it says why it is there, and "bus" → Vadaj only reads as an answer next to the word BRTS. The planner's suggestion rows had no meta line at all and grew one for this. **The facet is single-select and counts nine stations, not ten:** PDEU's `multiModal` is a parking amenity with no mode, so it has nothing to be faceted by, and no two modes overlap on more than one station — a multi-select would be a control with no question behind it. Two small corrections to §4.4 as written: *"railway"* was never the broken case (Kalupur Railway Station matches by name, and still ranks first), *"bus"* was — and there is no "airport-style" mode in the data.

- **2026-07-29** — **§8 phase 3 shipped.** A journey now ends by telling you how to leave: the destination's step-free exit and, on the 9 stations GMRC gives one, the onward connection with the Entry-Exit it uses — on the planned route's destination row and again on the live journey's final stop. One `<ExitGuidance>` over one derivation (`features/journey/exitGuidance.ts`), so the two surfaces cannot drift. Three decisions worth recording. **It renders for the whole ride, not only in the Arrived state as §4.2 said:** the moment to read which exit to walk toward is while you are still on the train, and "Arrived" barely exists as a distinct screen — it is the final-stop row, which is where this now lives. **GMRC's wording is kept verbatim, with only the "Entry-Exit 5 – " prefix stripped**, because the gate is already printed beside it; composing our own "BRTS via the skywalk" from that text would be invention, and the pairing of gate to connection is the fact a rider acts on (Ranip: GSRTC at Gate 2, BRTS at Gate 3 — a merged gate list would be wrong). The cost is Mahatma Mandir, whose one connection is 200 characters and wraps to eight lines at 320px; kept, because those characters are travellators and a bridge that opens onto the railway platform. **`multiModal` is not the same thing as a way onward:** PDEU's parking is an amenity, so a journey ending there shows its step-free exit alone and leaves parking to the station page.

- **2026-07-29** — **§8 phase 2 shipped.** The reference content of §4.5.1 is reachable: one generic `InfoPage` over a block model (`features/info/`), a topic registry, and eight deep-linkable `/you/:topic` routes behind `React.lazy`, reached from two new YOU sections placed **above** Preferences/Saved/History so live content isn't buried under three sections of Phase-4 stubs. Deviations from §4.5.1 as written, all deliberate: the **official GMRC app is a row, not a page** (two store URLs and a sentence do not earn a screen — the row opens the right store per platform); **prohibited items is reordered** to pets and luggage before the statutory lists, since GMRC's order is the Act's and the rider's question is "can I bring my dog"; **`metroInfo.luggage` renders on that page too**, because "can I bring this" includes size; and there is **no `numbered` list marker** — every GMRC list is a set of rules, not a sequence, and numbering a set invents an order. The block model gained `chips`, `linkList` and `definitions` beyond §4.6's five, each mapping to an idiom the Station Info tab already uses. §6.7 stays satisfiable: blocks hold strings, never composed JSX, so the Hindi/Gujarati resolver can swap them per language. **Bundle note worth keeping:** a named JSON import does *not* tree-shake per key — Vite emits one module per file, so any boot-path import drags the whole file into the main chunk. Confirmed by grepping the built output, not assumed; see `DISCREPANCIES.md`.

- **2026-07-28** — **§8 phase 1 shipped.** The Station Info tab now renders the physical station data: Entrances (a chip per open gate, GMRC's numbering, no invented denominator), Step-free access (lift→gate rows under a positive summary line), and Connections on the 10 stations that have one. `structure` joined the Station Overview tiles as a full-width fifth tile. One deviation from §4.4.1 as written: the Connections block is keyed off having *anything* to say rather than off `modes`, because `pdeu` has an amenity (parking) and no interchange — otherwise its only published fact would render nowhere. `plannedAmenities` stays unrendered; planned is not built.

- **2026-07-28** — Station & passenger information scraped from GMRC and given a delivery plan (§4.2, §4.4, §4.4.1, §4.5.1, §4.6, §5.4, §5.6, §6.7, §7.6, new §8). No UI written yet — scope-first, per this doc's operating rule.
  - **New data (§5.6).** `stationFacilities.json` — 53 of 54 stations, with structure (elevated/underground), operational gate numbers, lift→gate mapping, and multi-modal interchange on the 10 stations that have one, scraped from GMRC's entry/exit-gate page and its Multi Modal Integration page. `passengerInfo.json` — network facilities, do's & don'ts, prohibited items, emergency equipment, customer care, lost & found, official links. `metroInfo.json` extended after a full re-read of the fare-rules page. Regenerated by `scripts/fetch-station-facilities.mjs`, which throws on a page-shape change and cross-checks every MMI-cited Entry-Exit against that station's operational gates (all 10 pass, confirming the cross-page name mapping).
  - **Corrects an earlier claim.** The 2026-07-20 pass concluded GMRC publishes no per-station amenity data. The MMI page does, for 10 stations, and it is the most useful station data found so far. Still genuinely absent per station: toilets, Wi-Fi, ATMs, feeder-bus routes, and **gate landmarks** — the gate table numbers gates but never says which road each opens onto (§5.6, §8).
  - **Station Info tab (§4.4.1).** Gets Entrances / Step-free access / Connections blocks. Its doc-comment currently asserts no amenity data exists and is now half-wrong; logged in DISCREPANCIES.md. Step-free access is stated **positively** only — GMRC publishes where lifts are, so "Gate 2 has no lift" is a claim the source doesn't make.
  - **Reference content in YOU (§4.5.1).** Two new sections, nine topics, rendering through one generic `InfoPage` (§4.6) against a block model rather than nine hand-built screens. Deep-linkable at `/you/:topic`; JSON dynamically imported so reference prose stays out of the boot path.
  - **Last-mile guidance (§4.2).** The one place this data is in-the-moment rather than reference: the destination's step-free exit and onward connection, on the results screen and the Live Journey **Arrived** state.
  - **Emergency info deliberately kept in settings.** A Live Journey emergency affordance was considered and rejected — the platform's stop plunger and the train's passenger alarm are the right channel mid-emergency, and our own source says so.
  - **Localization gained a third surface (§6.7, old §6.7 renumbered §6.8).** GMRC's Do's & Don'ts and Prohibited Items posters are trilingual, so those two topics are **unblocked** for Hindi/Gujarati — unlike station names. The remaining topics are English-only at source and will say so rather than silently showing untranslated text. We do not translate GMRC's penalty or legal wording ourselves.
  - **New success criterion §7.6** — every physical station claim traces to a GMRC page; no invented amenity, no absence rendered as a negative.
- **2026-07-28** — Interchange sheet overflow and map-recenter accuracy fixed (§4.1); text selection disabled app-wide.
  - **Mid-snap cap.** `DraggableSheet`'s fitted mid snap was floored at `h * 0.12` (free to grow to ~88% of viewport) — on a 4-card interchange it measured 715px of an 812px screen, leaving only a 97px map strip once the search pill and line-status strip took their share, crowding out the floating Plan-route FAB and Recentre button. Floor raised to `h * 0.4`, capping the sheet at ~60% (~325px of map on the same viewport); a big interchange's departures now scroll within the mid snap instead of all fitting above the fold. Logged and resolved in DISCREPANCIES.md.
  - **Map recenter/pan accuracy.** `HomeScreen`'s `sheetInset` — the offset fed to `HomeMap` for both the recenter button and the pan-to-station nudge — was a fixed `midRatio` approximation that ignored the sheet's actual `snap` state, so recentering always shifted the map as if the sheet were at mid, even when it was collapsed or fitted taller on an interchange. Replaced with `sheetEdge`, the live rest-height measurement `DraggableSheet` already reports for the floating-control fit check (§4.1's "Map framing").
  - **Text selection.** `user-select: none` added to `html, body` (`index.css`), re-enabled on `input`/`textarea`, so dragging the sheet or a card no longer highlights text on desktop.
- **2026-07-27** — Journey summary rebuilt, station sheet header reworked, map paused while covered (§4.1, §4.2, §4.3, §4.6, §5.5).
  - **Ride time vs total time.** New `rideMinsOf` in the engine (§5.5) — boarding to arrival, excluding the *first* leg's platform wait but including interchange waits. Every "how long is this trip" figure in `JourneySummary` and `AllTrainsList` now uses it. Previously they showed `totalMins`, which is wait + ride, so a plan opened at 4am billed an 8-minute hop as a **2h 17m journey** in the screen's largest type. `AllTrainsList` also switched its row time to the train's own `departClockTime`; the door-to-door leave time is a minute or two earlier and made one train read as two different departures.
  - **Journey summary.** The row of three equal stats (fare / journey / arrival) became **one hero and one supporting column**: a large "Leave in N" countdown against a `depart → arrive` span with ride time and fare beneath. Arrival was dropped from the sheet header above, which had been printing the same clock time twice. The horizontal departure scroller became a **fixed four-cell segmented picker** — it can't be clipped by the fold and doesn't fight the sheet's vertical drag — and now renders for infeasible selections too, which is exactly when the rider needs to switch. The ticket note dropped to a quiet aside. Two columns rather than stacked because stacking pushed Start Journey below the mid snap.
  - **Station sheet header.** Line badge → **station photo** (new `stationImages.ts`, §4.6; no placeholder — a photo of the wrong station is worse than none). Star/"Favorite" → **bookmark** icon that widens to a "Saved" pill when set. Walking directions moved out of the body into a quiet header icon at the nearest station only; `openWalkingDirections` was extracted from `StationSheetActions` for it. Service status folded into the line chip instead of a separate pill, and the whole chip row now shows at **every** snap — the collapsed peek grows with its header rather than clipping it.
  - **Station sheet body.** The **Get Directions / Station Details** pair collapsed to a single **Start Journey** CTA, now *below* the departures rather than above them. It seeds the planner's **source** at the nearest station and its **destination** anywhere else. Removing "Station Details" leaves "View all →" as the sheet's only route away — logged in DISCREPANCIES.md.
  - **Shared `DepartureRow`** (§4.6) extracted from the two hand-rolled departure lists in `UpcomingTrains` and the station page's merged schedule, which had drifted apart. The merged schedule also now anchors the next train to the top of the scroller, measured from bounding rects rather than `offsetTop` (which is relative to the nearest positioned ancestor, not the scroller).
  - **Map paused while covered.** `HomeMap`/`LiveTrainsLayer` gained a `paused` prop and `DraggableSheet` an `onCoverageChange` callback. With the sheet fully raised or an overlay open, the map is hidden via `visibility` (never unmounted — that would drop pan/zoom) and the once-a-second train redraw stops. Coverage is read from the sheet's **live position**, not its `snap`, so the map stays painted for the whole animation. Resume ticks immediately rather than leaving stale positions up for another second.
- **2026-07-26** — Home station sheet rebuilt against a supplied design reference (§4.1, §4.4).
  - **Resting position.** The sheet now opens at **mid** instead of the collapsed peek, and its mid snap is **fitted to its own content** rather than a fixed fraction of the viewport. `DraggableSheet` gained an optional `midContentHeight`; `HomeScreen` measures the actions + departures block with a `ResizeObserver` and feeds it through. Measured rather than derived from constants because card count, chip-row wrapping and the location notice all move it. Floored at ~12% of the viewport so the sheet can never swallow the whole map. Verified at 375×812: a 4-card interchange shows all four cards with the schedule below the fold; a 2-card terminal rests ~150px lower.
  - **Header.** Chevron replaced by a **Favorite** toggle (`localStorage['metrothi-saved-stations']`, new `useSavedStations` hook). Eyebrow tinted with the accent; title 19→22px. Proximity chips (line · distance · walk) now show at *every* snap instead of only when collapsed. **Phase** and **Stop N of M** chips were removed from the sheet; they remain on the standalone `/stations/:id` hero. The inline Interchange tag moved out of the eyebrow into the chip row — at 375px it wrapped to a second line and pushed the chip row below the collapsed fold.
  - **Actions.** "From here / To here" replaced by **Get Directions** / **Station Details** (new `stationSheet/StationSheetActions`). The primary button splits on proximity: at the nearest station it is *Get Directions* and hands off to **Google Maps walking directions**; at any other station it is *Trip to here* and seeds the in-app planner. **Station Details** expands the sheet to full rather than routing to `/stations/:id` — the detail is already further down the same sheet. `StationDetailBody` gained `showActions` so its own button pair doesn't double up.
  - **Upcoming Trains.** New `stationSheet/UpcomingTrains` preview: next departure per direction per line, with a live marker and **View all →** to the station page. The full-day merged schedule continues below it — the preview is additive, nothing was removed.
  - **Gestures.** A header tap now **climbs** the snap ladder (collapsed → mid → full, full → mid) instead of dismissing to collapsed, matching the planned-route header. A tap on the **map surface** collapses the sheet (Leaflet's map-level `click`, so station markers — which don't bubble — still select and raise instead).
  - **Map framing.** `HomeMap`'s `bottomInset` now tracks the sheet's resting height instead of the hardcoded collapsed peek; the selected station's marker sits mid-band rather than against the sheet edge.
  - Known trade-offs and deferred items logged in DISCREPANCIES.md.
- **2026-07-24** — Home nearest-station sheet redesigned (§4.1). Killed the duplicated station identity (the sheet body's hero repeated the name/line the sticky header already showed) via a `showHero` flag — the standalone `/stations/:id` SEO page keeps its hero. The header now carries a chip row (line · distance · walk when collapsed; line · phase · stop, plus interchange/status, when expanded); the collapsed peek is sized to the header so the "From here / To here" buttons no longer peek through the fold. The per-line schedule dropped its two direction tabs for a single time-sorted list of both directions, highlighting the next train each way. Trip entry became two buttons — **From here** (source) / **To here** (destination) — that open the GO planner as an overlay through an in-app `home-plan-trip` event; this also fixed the old "Plan Trip From Here" / "I want to catch this train" buttons, which navigated to a non-existent `/go` route and silently no-op'd. The redundant "catch this train" button was removed. Dead code and the SEO-page overlay gap are logged in DISCREPANCIES.md.
- **2026-07-23** — Added the Localization & Multilingual Support plan (§6, new section; Success Criteria renumbered §6→§7). Specifies English/Hindi/Gujarati via `react-i18next`, the split between UI-chrome and name-data translation surfaces, the Devanagari/Gujarati font requirement, a settings-screen selector (§4.5), and a 7-phase implementation plan. Blocked only on an official GMRC name source (§6.7). No code written yet — scope-first, per the doc's operating rule.
- **2026-07-23** — Map viewport constrained (§4.3). The home map is now fenced to the Ahmedabad–Gandhinagar area via Leaflet `maxBounds` + `maxBoundsViscosity: 1.0` (hard fence — the drag stops solid at the edge, no bounce-back), with a `minZoom` floor locking zoom-out and `maxZoom: 18` for close inspection.
- **2026-07-23** — Map rendering upgraded to real track geometry (§4.3). Station markers are now snapped onto the drawn track (previously they floated up to ~52m off the line once polylines switched from station chords to OSM geometry). Terminal markers are rotated perpendicular to the track. The live-trains layer — which had silently never rendered because it attached to a non-existent Leaflet renderer property (`_renderer._svg`; fixed to `_rootGroup`) — now works, and each train shows a direction-of-travel arrowhead. A stale `move zoom` listener leak in the same layer was also fixed, and three pre-existing TypeScript build errors cleared. See DISCREPANCIES.md (Resolved / Fixed, 2026-07-23).
- **2026-07-20** — Fare engine rebuilt from scratch on real GMRC data (§5.4). Previous fare logic was station-count-based, self-labelled a placeholder, and capped at ₹25 with no citation; it undercharged long trips by up to ₹15. CSC/NCMC 10% discount and the Phase 1/Phase 2 ticket restriction confirmed against GMRC's official pages and recorded in `app/src/data/metroInfo.json`.
